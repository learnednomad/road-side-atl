---
name: bmad-sprint-orchestrator
description: 'Semi-autonomous sprint execution engine that analyzes story dependencies, parallelizes implementation via worktrees, and manages the full dev-review-merge cycle. Use when the user says "run sprint", "execute sprint", or "orchestrate sprint"'
---

Follow the instructions in ./workflow.md.
