# RoadSide GA — Code Review (2026-06-21)

Automated 15-dimension review with adversarial verification of every finding (each finding was independently re-checked against the actual code; false positives dropped).

**70 confirmed findings** (1 critical · 15 high · 43 medium · 11 low) — 1 rejected of 71 raw across 15 dimensions.

## Findings by dimension

| Dimension | Count |
|---|---|
| maintainability | 7 |
| b2b-credit | 6 |
| tests | 6 |
| secrets-ratelimit-pii | 6 |
| notifications-webhooks-out | 6 |
| reconciliation | 5 |
| performance | 5 |
| stripe-webhooks | 4 |
| payout-commission | 4 |
| dispatch | 4 |
| onboarding | 4 |
| authz | 4 |
| realtime | 3 |
| validation-injection | 3 |
| pricing-engine | 3 |


## 🔴 CRITICAL

### WebSocket auth trusts client-supplied userId/role — any client can impersonate admin or any user
- **Location:** `server/websocket/server.ts:24`  
- **Dimension:** WebSocket & push realtime · **Category:** security · **Reviewer confidence:** high
- **Evidence:** The only "auth" is reading unverified fields straight off the wire: `if (msg.type === "auth" && msg.userId && msg.role) { userId = msg.userId; role = msg.role; ... addConnection(userId!, role!, ws); }`. There is no session/JWT/cookie check at the HTTP upgrade (handleUpgrade only checks `req.url === "/ws"`) nor in the auth message. The client hook sends it verbatim: `ws.send(JSON.stringify({ type: "auth", userId, role }))` (lib/hooks/use-websocket.ts:32). Any attacker can open ws://host/ws and send `{type:"auth",userId:"x",role:"admin"}` to join the admin fan-out. broadcastToAdmins/broadcastToProvider/broadcastToUser then deliver sensitive PII and financial events: provider:job_assigned carries `contactPhone`, `address`, `latitude`, `longitude`; booking:created carries `contactName`; payout:transfer_failed, payment:refunded (with `refundAmount` in cents) are admin-broadcast (types.ts:5,16,17). Claiming a victim's userId or providerId streams that user's job/booking data to the attacker.
- **Fix:** Authenticate the WebSocket from the trusted session, not the message. In handleUpgrade, parse the auth/session cookie from `req.headers.cookie`, validate it server-side, and attach the resolved {userId, role} to the socket; reject the upgrade (socket.destroy) if invalid. Ignore any userId/role sent in the auth message — derive them only from the validated session. For the public /track room, scope it to the bookingId only and never accept a client-asserted role of "admin"/"provider".
- **Verified:** Confirmed real and unmitigated. server/websocket/server.ts:24 authenticates purely off the wire: `if (msg.type === "auth" && msg.userId && msg.role) { userId = msg.userId; role = msg.role; ... addConnection(userId!, role!, ws) }`. The HTTP upgrade path (handleUpgrade, lines 58-68) only checks `req.url !== "/ws"` and never reads `req.headers.cookie` or validates any session/JWT. server-custom.ts:25…


## 🟠 HIGH

### Provider onboarding/suspension gate is bypassed for Bearer-token (mobile) clients
- **Location:** `server/api/middleware/onboarding.ts:18`  
- **Dimension:** AuthN/Z & middleware · **Category:** security · **Reviewer confidence:** high
- **Evidence:** requireOnboardingComplete resolves identity with cookie-only NextAuth: `const session = await auth(); if (!session?.user?.id || session.user.role !== "provider") { return next(); }` (onboarding.ts:18-21). It is the only active-status enforcement on dispatch/earnings/invoice routes (index.ts:58-63 `app.use("/provider/jobs/*", requireOnboardingComplete)` etc.). But `resolveUser` in auth.ts:23-52 also authenticates via `Authorization: Bearer <jwt>` — the documented mobile path — which `auth()` does NOT read. So for a mobile request `auth()` returns null, the gate falls through to `return next()`, and the downstream `requireProvider` (provider.ts:32) authenticates the same user via Bearer. A provider with status 'suspended' (e.g. auto-suspended for not completing Stripe Connect in admin-payouts.ts mark-paid) or 'pending' onboarding can still hit /provider/jobs, /jobs/:id/status, /stats, /earnings/*, /location and /provider/invoices/* over mobile, defeating suspension.
- **Fix:** Make requireOnboardingComplete resolve identity the same way as the rest of the API — call `resolveUser(c)` (which supports Bearer) instead of `auth()`, so the active/migration-bypass check runs for token-auth clients too. Apply the same fix anywhere middleware uses bare `auth()` for gating.
- **Verified:** Confirmed in code. requireOnboardingComplete (server/api/middleware/onboarding.ts:18) resolves identity via bare auth() which only reads NextAuth cookies. The canonical resolver resolveUser (auth.ts:21-53) additionally accepts Authorization: Bearer JWTs — the documented mobile path (per CLAUDE.md). On a Bearer request auth() returns null, so onboarding.ts:19-21 falls through with return next(), sk…

### Cancelling a NET booking never reverses the credit charge — AR balance and consumed credit stay permanently inflated
- **Location:** `server/api/lib/b2b-booking.ts:193`  
- **Dimension:** B2B credit/invoicing/ledger · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** On creation every non-prepaid booking inserts a `charge` ledger row and does `currentBalanceCents = balance + estimatedPrice` (lines 193-204). The cancel paths (bookings.ts:324-328 sets status 'cancelled'; admin.ts:441 cancelled branch) only flip status — neither inserts a reversing `adjustment`/credit row nor decrements currentBalanceCents. The monthly invoice excludes cancelled bookings (only status='completed', invoice-generator.ts:155), so the charge is billed to nobody yet the balance keeps it forever. Net AR can never return to 0 and (once limits work) cancelled bookings silently eat the credit limit.
- **Fix:** On B2B booking cancellation, within a tx insert a `b2bCreditTransactions` row (type 'adjustment', amountCents = -estimatedPrice, bookingId) and decrement currentBalanceCents, but only if a 'charge' for that bookingId exists and no reversal already exists (idempotent).
- **Verified:** Confirmed in actual code. createB2bBooking (server/api/lib/b2b-booking.ts:180-205) charges every non-prepaid (NET) account on creation: it inserts a b2bCreditTransactions row type 'charge' amountCents=estimatedPrice (lines 193-199) and sets currentBalanceCents = balance + estimatedPrice (line 202), inside a row-locked tx. Both cancellation paths only flip status: the user endpoint (server/api/rout…

### Monthly-invoice period filter coerces end date to midnight, permanently dropping last-day bookings (revenue leak)
- **Location:** `server/api/lib/invoice-generator.ts:158`  
- **Dimension:** B2B credit/invoicing/ledger · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** `sql\`${bookings.updatedAt} <= ${billingPeriodEnd}\`` where billingPeriodEnd is a 'YYYY-MM-DD' string. Postgres casts it to timestamp '2024-06-30 00:00:00', so a booking completed 2024-06-30 14:00 fails `<= '2024-06-30'`. With calendar-month periods the next period starts '2024-07-01' and that booking also fails `>= '2024-07-01'` (line 157), so it falls into a gap and is NEVER invoiced — uncollected revenue.
- **Fix:** Use an exclusive upper bound on the day after the period end, e.g. `updatedAt < (billingPeriodEnd::date + interval '1 day')`, or cast end to end-of-day. Apply the same care to the start bound.
- **Verified:** Confirmed from actual code. server/api/lib/invoice-generator.ts:158 compares bookings.updatedAt (a full timestamp, db/schema/bookings.ts:88 `timestamp(..., {mode:"date"})`) against billingPeriodEnd, which is a YYYY-MM-DD string enforced by generateB2bInvoiceSchema (lib/validators.ts:558-559). Postgres coerces '2024-06-30' to timestamp '2024-06-30 00:00:00', so a booking completed 2024-06-30 14:00 …

### AR ledger drifts when finalPrice differs from estimatedPrice (charge vs invoice/payment mismatch)
- **Location:** `server/api/lib/b2b-booking.ts:196`  
- **Dimension:** B2B credit/invoicing/ledger · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** Credit is charged at creation using `amountCents: estimatedPrice` (b2b-booking.ts:195-198) and balance bumped by estimatedPrice. But the monthly invoice lines bill `row.booking.finalPrice || row.booking.estimatedPrice` (invoice-generator.ts:167), and finalPrice is set later by admin price adjustment (admin.ts:615) or customer quote approval (bookings.ts:391). Stripe invoice.paid then reduces balance by `amount_paid` = the finalPrice-based invoice total (webhook-handlers/invoice-paid.ts:40-46). So after full payment the balance retains (estimatedPrice - finalPrice) per adjusted booking and never zeros out.
- **Fix:** Accrue/adjust the credit ledger from the billed amount: either charge against finalPrice, or on price adjustment/completion post an `adjustment` row for (finalPrice - estimatedPrice) so the ledger total matches what is actually invoiced and paid.
- **Verified:** Confirmed from the code. b2b-booking.ts:196/202 accrues the NET credit ledger and balance using estimatedPrice (type "charge", amountCents: estimatedPrice; balance += estimatedPrice). The monthly invoice (invoice-generator.ts:167) bills finalPrice || estimatedPrice, and the invoice.paid webhook (invoice-paid.ts:40/46) pays the balance down by amount_paid = the finalPrice-based invoice total. final…

### Offer-expiry cron clobbers accepted/in-progress bookings (revert UPDATE is not state-conditional)
- **Location:** `server/api/lib/offer-expiry.ts:44`  
- **Dimension:** Dispatch V1/V2 & cascade · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** After SELECTing expired bookings, the revert is unconditional: `await db.update(bookings).set({ providerId: null, status: "confirmed", offerExpiresAt: null, ... }).where(eq(bookings.id, booking.id));` (lines 44-52). There is no `WHERE status='dispatched' AND offerExpiresAt<=now` guard and no RETURNING check. Meanwhile the provider accept handler also writes unconditionally: provider.ts:141-145 `.set({ status: "in_progress", offerExpiresAt: null }).where(eq(bookings.id, bookingId))` after only a JS-level status check (provider.ts:137). If a provider accepts in the window between the cron's SELECT and its UPDATE, the cron overwrites the just-accepted `in_progress` booking back to `confirmed`/`providerId=null` and then cascades a new offer to a different provider — the accepting provider silently loses the job and the booking gets double-offered. The same lack of a conditional update means two overlapping 15s cron runs (offer-expiry-check, cron.ts:83) can both SELECT the same still-`dispatched` expired booking and both revert+cascade, doubling dispatchAttempt and sending two competing offers.
- **Fix:** Make the revert atomic and idempotent: `UPDATE bookings SET ... WHERE id=? AND status='dispatched' AND offerExpiresAt <= now() RETURNING *`, and only run the cascade/notifications when a row was actually returned. Apply the same conditional guard (status='dispatched') to the accept/reject updates in provider.ts.
- **Verified:** Verified in source. offer-expiry.ts:26-52 SELECTs dispatched/expired bookings then issues an UNCONDITIONAL revert UPDATE (WHERE eq(id) only) to status='confirmed', providerId=null, with no status guard, no RETURNING check, and no transaction/row lock around the SELECT+UPDATE. provider.ts:129-145 accept handler does a JS-only status check (line 137) then an unconditional UPDATE to 'in_progress' (WH…

### V2/V1 dispatch assigns providers without guarding current booking status (concurrent/stale dispatch hijack)
- **Location:** `server/api/lib/auto-dispatch-v2.ts:224`  
- **Dimension:** Dispatch V1/V2 & cascade · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** V2 sets the offer unconditionally: `await db.update(bookings).set({ providerId: best.providerId, status: "dispatched", offerExpiresAt, dispatchAttempt: attempt, ... }).where(eq(bookings.id, bookingId));` (lines 224-233). V1 does the same (auto-dispatch.ts:134-141). Nothing verifies the booking is still in a dispatchable state at write time. Multiple triggers can fire for one booking concurrently — on-create dispatch (bookings.ts:174), the mechanic-pre-dispatch cron which selects `status='confirmed' AND providerId IS NULL` (mechanic-pre-dispatch.ts:39-40 then dispatchBooking line 55), and reject/expiry cascades. A stale cascade started before a provider accepted will overwrite an `in_progress` booking back to `dispatched` with a different provider, or two triggers will both assign/notify different providers for the same booking (last-write-wins on providerId, but both providers receive job_assigned).
- **Fix:** Condition the assignment update on the booking still being dispatchable, e.g. `WHERE id=? AND status IN ('confirmed','pending') AND providerId IS NULL` (V2 also allowing the cascade source state), use RETURNING, and abort the offer/notifications if no row was updated.
- **Verified:** Confirmed against the actual code. The assignment UPDATE at auto-dispatch-v2.ts:224-233 (and V1 auto-dispatch.ts:134-141) writes providerId/status='dispatched'/offerExpiresAt unconditionally via `.where(eq(bookings.id, bookingId))` with no guard on the booking's current state. The dispatch subsystem has several concurrent triggers (on-create bookings.ts:174, mechanic cron mechanic-pre-dispatch.ts:…

### Failed background checks and rejected doc-steps are silently dropped (no in_progress→rejected transition)
- **Location:** `server/api/lib/onboarding-state-machine.ts:22`  
- **Dimension:** Onboarding state machine · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** STEP_TRANSITIONS defines `in_progress: ["pending_review", "complete"]` — "rejected" is absent. The background_check step is created in `in_progress` (onboarding.ts:174-186) and stays there. When Checkr returns a failing result, webhooks.ts:931-932 computes `newStepStatus` = "rejected" (CHECKR_STATUS_MAP maps suspended/adverse_action/post_adverse_action → rejected), but webhooks.ts:944 `if (isValidStepTransition(step.status, newStepStatus))` is false for in_progress→rejected, so the whole status/rejectionReason update block is skipped: the step remains `in_progress`, no rejectionReason recorded, yet broadcastToUser still sends newStatus:'rejected' (webhooks.ts:978-985). The same gap affects admin step review: insurance/certifications steps are put into `in_progress` by POST /steps/:stepId/submit (onboarding.ts:1111-1114), and admin-providers.ts:974 blocks an admin from rejecting them (in_progress→rejected returns 400). There is effectively no working path to reject these steps.
- **Fix:** Add "rejected" to the in_progress transition list in onboarding-state-machine.ts (`in_progress: ["pending_review", "complete", "rejected"]`), and have the Checkr webhook set rejectionReason + completedAt:null accordingly. Verify admin step-review and notifications then reflect the rejected state.
- **Verified:** Verified against actual code. onboarding-state-machine.ts:22 defines `in_progress: ["pending_review","complete"]` with "rejected" absent. background_check is created in in_progress (onboarding.ts:177). CHECKR_STATUS_MAP (lib/constants.ts:185-187) maps suspended/adverse_action/post_adverse_action → "rejected". In webhooks.ts:944 the status/rejectionReason update is guarded by isValidStepTransition(…

### flat_per_job payout is not capped at the booking price (only floored at 0)
- **Location:** `server/api/lib/payout-calculator.ts:26`  
- **Dimension:** Payout & commission correctness · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** `if (provider.commissionType === "flat_per_job") { amount = provider.flatFeeAmount || 0; }` and the function ends with `return Math.max(0, amount);` (line 45) — only a lower bound. If `flatFeeAmount > effectivePrice`, providerAmount exceeds the price. At checkout the application fee is clamped: `applicationFeeAmount = Math.max(0, booking.estimatedPrice - providerShare)` (payments.ts:160) → fee becomes 0, so Stripe transfers the FULL charge to the provider, while the payout RECORD stores `flatFeeAmount` (a larger number) — recorded payout != actual transfer, and the platform earns 0 / loses money. On the legacy transfer path (line 262-271) `transfers.create({ amount: providerAmount })` would attempt to pay out more cents than were collected.
- **Fix:** Clamp the flat fee to the price: `amount = Math.min(effectivePrice, provider.flatFeeAmount || 0)`, or generally `return Math.min(effectivePrice, Math.max(0, amount))` at the end of computeProviderAmount.
- **Verified:** Confirmed from source. computeProviderAmount (server/api/lib/payout-calculator.ts:26-27) returns provider.flatFeeAmount uncapped for the flat_per_job branch, and the function's only bound is Math.max(0, amount) at line 45 (no upper cap to effectivePrice). All other branches are percentage-of-price so cannot exceed the price; flat_per_job is the sole branch that can. When flatFeeAmount > effectiveP…

### No 'error' listener on WebSocket sockets — a socket error crashes the entire Next.js process
- **Location:** `server/websocket/server.ts:11`  
- **Dimension:** WebSocket & push realtime · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** In `wss.on("connection", (ws) => {...})` only `message` and `close` listeners are attached; there is no `ws.on("error", ...)` and no `wss.on("error", ...)` (grep for `.on("error"` / `terminate` / `pong` across server/websocket and server-custom returns nothing). The `ws` library's WebSocket is an EventEmitter and emits 'error' on events like ECONNRESET / protocol errors; an 'error' event with no listener makes Node throw an uncaught exception. Because the WS server shares the same process as the HTTP server (server-custom.ts runs `createServer` and `createWebSocketServer()` together), one client-triggered socket error takes down the whole site, not just the connection.
- **Fix:** Add `ws.on("error", (err) => { logger.error(...); ws.terminate(); })` inside the connection handler and `wss.on("error", ...)` on the server, so socket-level errors are handled instead of bubbling to an uncaught exception.
- **Verified:** Confirmed from code. server/websocket/server.ts:11 connection handler attaches only 'message' (L20) and 'close' (L36,L52) listeners — no ws.on('error') and no wss.on('error') (repo-wide grep for .on('error' returns nothing). The ws library (^8.19.0) WebSocket is an EventEmitter that emits 'error' on socket failures (ECONNRESET, protocol errors); an unhandled 'error' event throws in Node, becoming …

### processPendingPayouts can double-pay providers when DB write fails (idempotency key expires before next run)
- **Location:** `server/api/lib/reconciliation.ts:680`  
- **Dimension:** Reconciliation scheduled jobs · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** Transfer is created then the row is marked paid in a separate, non-atomic step:
```
const transfer = await stripe.transfers.create({...}, { idempotencyKey: `payout-${payout.id}` });
await db.update(providerPayouts).set({ status: "paid", ... }).where(eq(providerPayouts.id, payout.id));
```
If the Stripe call succeeds but the subsequent DB update fails (DB timeout/connection drop), the payout row stays `status: 'pending'`. The job (`process-pending-payouts`, cron.ts:99) runs every `24 * HOUR`. Stripe retains idempotency-key results for only 24 hours, so the next run — at or just past the 24h boundary — finds the still-pending row, calls `transfers.create` again with the now-expired key, and Stripe creates a SECOND transfer. The `uniqStandardPerBooking` index (provider-payouts.ts:35) only backstops duplicate payout *rows*, not duplicate Stripe transfers on the same row.
- **Fix:** Make the cron interval safely shorter than Stripe's 24h idempotency window (e.g. 6–12h) so a retry always reuses the key, and/or guard against partial state: before creating a transfer, re-check `stripeTransferId IS NULL`; record the intended transfer attempt (and idempotency key) before calling Stripe; on DB-write failure, persist the returned `transfer.id` via a retry/outbox so the row never silently stays pending after a successful transfer.
- **Verified:** Confirmed against the actual code. server/api/lib/reconciliation.ts:680-699 creates a Stripe transfer with idempotencyKey `payout-${payout.id}` then performs the status->'paid'/stripeTransferId DB update as a separate, non-atomic step. The catch block (702-709) only logs/counts failures, so if Stripe succeeds but the DB update fails (timeout/connection drop) the row stays status='pending' with str…

### Extension handlers swallow errors, causing event to be marked processed and money events lost permanently
- **Location:** `server/api/lib/webhook-handlers/invoice-paid.ts:49`  
- **Dimension:** Stripe webhooks & idempotency · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** handleInvoicePaid wraps the entire B2B credit ledger write in try/catch and only logs on failure: `} catch (err) { logger.error("[Webhook] invoice.paid B2B credit update failed", ...) }`. Because it returns normally rather than throwing, control returns to webhooks.ts where the default branch (`await handler(event)`, line 852) completes with unhandled=false and `markEventProcessed(event.id, "stripe", event.type, "processed")` runs (line 860). Stripe receives 200 and never retries. If the ledger transaction fails for any transient reason (deadlock, connection drop), the customer's paid Stripe invoice is NEVER credited against their b2bAccounts.currentBalanceCents — the payment is silently lost from the ledger. The identical fail-open pattern is in subscription.ts:44 (`handleSubscriptionUpsert`) and subscription.ts:53, so a paid membership subscription can fail to activate while Stripe is told everything succeeded.
- **Fix:** Money-applying extension handlers must re-throw on unexpected failures (only swallow the intentional no-op early-returns) so the webhook returns non-2xx, markEventProcessed is NOT reached, and Stripe redelivers. Distinguish 'not applicable' (return) from 'failed to apply' (throw).
- **Verified:** Confirmed in actual code. invoice-paid.ts:20-51 wraps the whole B2B credit-ledger transaction in try/catch that only logs (line 50) and returns void normally. webhooks.ts:852 dispatches via `await handler(event)` with no surrounding try/catch, so on a swallowed error `unhandled` stays false and markEventProcessed(...,"processed") runs at line 860, returning HTTP 200. Stripe therefore never retries…

### Checkr webhook marks event processed BEFORE handling it, losing report.completed results on any downstream error
- **Location:** `server/api/routes/webhooks.ts:904`  
- **Dimension:** Stripe webhooks & idempotency · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** In the Checkr handler the dedup record is written up front: `await markEventProcessed(event.id, "checkr");` (line 904) executes before the report.completed processing block (lines 907-1013) that updates onboardingSteps status, transitions state, and notifies. If any of those subsequent awaits throw (DB error, broadcast failure), Hono returns 500 and Checkr retries — but `isEventProcessed(event.id, "checkr")` (line 901) now returns true, so the retry short-circuits to `return c.json({ ok: true })` and the background-check result is never applied. The provider's onboarding step stays stuck forever. This is the opposite ordering from the Stripe handler, which marks AFTER processing (line 860).
- **Fix:** Move `markEventProcessed` for Checkr to after the report.completed processing succeeds (mirroring the Stripe path at line 860), or wrap claim+process so a failure does not leave the event recorded as processed.
- **Verified:** Confirmed in server/api/routes/webhooks.ts. markEventProcessed(event.id, "checkr") is called at line 904 BEFORE the report.completed processing block (lines 906-1012). markEventProcessed (lines 43-57) does an independent insert with onConflictDoNothing and its own try/catch — it is not in a transaction with the downstream work, so the dedup row commits regardless of what follows. Several downstrea…

### V2 offer-expiry + cascade has no end-to-end test; revert UPDATE lacks a status guard (accept/expiry race)
- **Location:** `server/api/lib/offer-expiry.ts:44`  
- **Dimension:** Test coverage gaps · **Category:** test-gap · **Reviewer confidence:** high
- **Evidence:** processExpiredOffers() is never imported by any test — dispatch-v2.test.ts (line 12) MOCKS autoDispatchBookingV2 and only tests dispatch-router delegation; its "Offer Expiry Logic" describe asserts inline literals, not real code. The expiry revert is unguarded:
  await db.update(bookings).set({ providerId: null, status: "confirmed", offerExpiresAt: null, ... }).where(eq(bookings.id, booking.id));
There is no WHERE guard on status="dispatched"/offerExpiresAt still set, so if a provider accepts between the SELECT (line 26) and this UPDATE, the cron clobbers the accepted booking back to confirmed and strips providerId. The cascade then re-offers an already-accepted job (double-assignment).
- **Fix:** Add an integration test for processExpiredOffers that: (a) expires one booking and asserts revert + dispatchLogs outcome="expired" + cascade attempt incremented + manual-needed at MAX_DISPATCH_CASCADE_ATTEMPTS; (b) simulates accept-then-expire and asserts the accepted booking is NOT reverted. Then make the revert UPDATE conditional: .where(and(eq(bookings.id, booking.id), eq(bookings.status,"dispatched"), eq(bookings.offerExpiresAt, booking.offerExpiresAt))). The same unguarded clobber exists in auto-dispatch-v2.ts:224-233.
- **Verified:** Confirmed from source. (1) Test-gap: processExpiredOffers is referenced only in server/cron.ts:86-87 and its own definition — never imported by any test. tests/unit/dispatch-v2.test.ts mocks autoDispatchBookingV2 (lines 11-13), tests only router delegation, and its "Offer Expiry Logic" block (lines 134-174) asserts inline literals, exercising zero lines of offer-expiry.ts; the line 143 "race condi…

### B2B NET credit-limit deduction (guard + ledger + balance bump) is completely untested
- **Location:** `server/api/lib/b2b-booking.ts:180`  
- **Dimension:** Test coverage gaps · **Category:** test-gap · **Reviewer confidence:** high
- **Evidence:** grep for createB2bBooking across tests/ returns nothing. The money-critical transaction draws down credit inside a row-locked tx:
  const [acct] = await tx.select(...).for("update");
  if (limit > 0 && balance + estimatedPrice > limit) throw new CreditLimitError(...);
  ...insert b2bCreditTransactions {type:"charge", amountCents: estimatedPrice}...
  ...update currentBalanceCents: balance + estimatedPrice...
estimatedPrice here already includes the additive towing surcharge (line 127) and any priceOverrideCents. The limit-0 = "track don't cap" semantics (line 189) and the isNet derivation (line 177) have zero coverage.
- **Fix:** Add unit tests asserting: (1) NET booking inserts a 'charge' ledger row equal to estimatedPrice INCLUDING towing additive and bumps currentBalanceCents by the same; (2) booking that pushes balance over a positive creditLimitCents throws CreditLimitError with no booking/ledger row written (tx rollback); (3) limit=0 accrues balance without capping; (4) prepaid path writes no ledger row. Catches credit overruns, double-charge, and towing-not-charged regressions.
- **Verified:** Confirmed from source. server/api/lib/b2b-booking.ts:180-205 implements the described NET credit-drawdown inside db.transaction: row-locked balance read via .for("update") (182-186), CreditLimitError guard `if (limit > 0 && balance + estimatedPrice > limit)` at line 189, b2bCreditTransactions {type:"charge", amountCents: estimatedPrice} insert (193-199), and currentBalanceCents = balance + estimat…

### /push/resubscribe is unauthenticated and unvalidated — subscription hijack + crash
- **Location:** `server/api/routes/push.ts:111`  
- **Dimension:** Input validation & injection · **Category:** security · **Reviewer confidence:** high
- **Evidence:** `app.post("/resubscribe", async (c) => { const body = await c.req.json(); const { oldEndpoint, newSubscription } = body; ... }` has NO `requireAuth` middleware (unlike `/subscribe` at push.ts:36 and `/unsubscribe` at push.ts:78 which both pass `requireAuth`) and NO zod parse. It looks up any subscription by attacker-supplied `oldEndpoint` and overwrites its `endpoint`/`keys` (push.ts:117-132). An unauthenticated caller who knows/guesses a victim's endpoint can redirect that user's push notifications to an attacker-controlled endpoint. Additionally `newSubscription.keys.p256dh` (push.ts:127) is dereferenced with no validation, so a body with `newSubscription` but no `keys` throws an unhandled 500.
- **Fix:** Add `requireAuth` to `/resubscribe` and scope the update to the authenticated user's own subscriptions (`eq(pushSubscriptions.userId, user.id)`), and validate the body with a zod schema (reuse `subscriptionSchema` used at push.ts:38) before touching `newSubscription.keys`.
- **Verified:** Confirmed in /root/road-side-atl/server/api/routes/push.ts. The /resubscribe handler (line 111) registers with NO requireAuth middleware, unlike /subscribe (line 34) and /unsubscribe (line 78) which both pass requireAuth. It reads the body via raw c.req.json() (line 112) with NO zod validation (contrast /subscribe line 38 subscriptionSchema.safeParse). It looks up any subscription by attacker-supp…


## 🟡 MEDIUM

### Unauthenticated /push/resubscribe lets anyone re-point another user's push subscription
- **Location:** `server/api/routes/push.ts:111`  
- **Dimension:** AuthN/Z & middleware · **Category:** security · **Reviewer confidence:** high
- **Evidence:** `app.post("/resubscribe", async (c) => {...})` has NO auth middleware (every other push route uses requireAuth). It looks up a subscription solely by `oldEndpoint` and overwrites its endpoint+keys with attacker-supplied `newSubscription` values, with no ownership check. Separately, `POST /subscribe` (push.ts:46-56) matches an existing row by endpoint only and rewrites `userId: user.id`, re-associating an existing subscription to the caller. An attacker who learns/guesses an endpoint can hijack or tamper with a victim's notification delivery.
- **Fix:** Require auth on /resubscribe and scope the update to `and(eq(endpoint, oldEndpoint), eq(userId, user.id))`. In /subscribe, only update the existing row when `existing.userId === user.id` (otherwise insert a new row), so an endpoint cannot be reassigned across accounts.
- **Verified:** Confirmed from actual code. server/api/routes/push.ts:111 `app.post("/resubscribe", async (c) => {...})` has NO requireAuth middleware, unlike the other three routes in the file (/subscribe L34, /unsubscribe L78, /status L97 all use requireAuth). The handler (L117-132) finds a subscription by oldEndpoint ONLY and overwrites its endpoint+keys with attacker-supplied newSubscription values, with no u…

### Credit-limit enforcement is dead code — no API ever sets a positive limit, so all NET accounts have unlimited credit
- **Location:** `server/api/lib/b2b-booking.ts:189`  
- **Dimension:** B2B credit/invoicing/ledger · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** Enforcement guard: `if (limit > 0 && balance + estimatedPrice > limit) { throw new CreditLimitError(...) }`. But `creditLimitCents` defaults to 0 (db/schema/b2b-accounts.ts:65 `creditLimitCents: integer(...).notNull().default(0)`) and grep shows NO writer anywhere: createB2bAccountSchema (validators.ts ~line) and updateB2bAccountSchema only accept companyName/contact/billing/paymentTerms/notes — never creditLimitCents. So `limit` is always 0, the `limit > 0` condition is never true, and CreditLimitError can never fire. Every NET (net_30/net_60) account effectively has infinite credit.
- **Fix:** Add `creditLimitCents` (int >= 0) to createB2bAccountSchema/updateB2bAccountSchema and wire it into the POST/PATCH /:id handlers in b2b-accounts.ts so admins can set a cap. Until then the entire credit-limit subsystem (lock, ledger guard, 402 responses) provides no protection.
- **Verified:** Verified from source. b2b-booking.ts:189 guards with `if (limit > 0 && ...)` where limit comes from b2bAccounts.creditLimitCents, which defaults to 0 (schema b2b-accounts.ts:65) and migration 0003. No application writer exists: createB2bAccountSchema/updateB2bAccountSchema (lib/validators.ts:341-361) omit creditLimitCents, and the POST (b2b-accounts.ts:107-119) and PATCH (:177-188) handlers explic…

### Manual /credit/payment has no idempotency or dedup against the Stripe invoice.paid webhook — balance can be double-reduced
- **Location:** `server/api/routes/b2b-accounts.ts:479`  
- **Dimension:** B2B credit/invoicing/ledger · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** The handler inserts a 'payment' row and sets `currentBalanceCents = balance - amountCents` (lines 478-489) without checking for an existing payment for parsed.data.invoiceId. The Stripe handler explicitly guards against this for itself (invoice-paid.ts:31-36 skips if a payment row already exists for the invoiceId), but the manual endpoint does no such check — so recording a manual payment for an invoice that is also paid via Stripe (or double-submitting the form) reduces the balance twice, driving AR artificially negative.
- **Fix:** In the tx, when invoiceId is provided, first check for an existing `b2bCreditTransactions` row with that invoiceId+type='payment' and reject/no-op if present; consider an optional amount<=balance sanity guard.
- **Verified:** Confirmed against the actual code. The manual endpoint POST /:id/credit/payment (server/api/routes/b2b-accounts.ts:464-506) locks the account, computes newBalance = balance - amountCents, inserts a type='payment' row with the provided invoiceId, and reduces the balance — with NO dedup check on invoiceId. By contrast, the Stripe handler handleInvoicePaid (server/api/lib/webhook-handlers/invoice-pai…

### Overlapping billing periods double-invoice the same completed bookings
- **Location:** `server/api/lib/invoice-generator.ts:134`  
- **Dimension:** B2B credit/invoicing/ledger · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** Duplicate guard only matches an EXACT (billingPeriodStart AND billingPeriodEnd) pair (lines 134-141). The uninvoiced filter uses `leftJoin(invoices, invoices.bookingId == bookings.id)` + `isNull(invoices.id)` (line 159), but monthly invoices are inserted with NO bookingId (createB2bMonthlyInvoice insert sets tenantId, not bookingId), so previously monthly-invoiced bookings are not detected by that join. Running periods Jun 1-30 then Jun 15-Jul 15 bills the Jun 15-30 completed bookings on both invoices.
- **Fix:** Track which bookings a monthly invoice covered (persist bookingIds on the invoice or a join table) and exclude already-covered bookings, and/or reject overlapping periods rather than only exact-duplicate periods.
- **Verified:** Confirmed from code. invoices.bookingId is nullable (db/schema/invoices.ts:42-43) and createB2bMonthlyInvoice's insert (invoice-generator.ts:189-206) never sets bookingId — only tenantId + billingPeriodStart/End. The uninvoiced filter (line 152, 159) joins on invoices.bookingId == bookings.id and keeps rows where isNull(invoices.id); since monthly invoices have null bookingId, bookings already cov…

### V1 and V2 compute estimatedPayout differently (duplicated logic has drifted)
- **Location:** `server/api/lib/auto-dispatch-v2.ts:268`  
- **Dimension:** Dispatch V1/V2 & cascade · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** For the service-commission branch V1 takes the max of service-level and provider-level commission: auto-dispatch.ts:165-167 `const serviceProviderAmount = estimatedPrice - Math.round(estimatedPrice * service.commissionRate / 10000); const providerLevelAmount = Math.round(estimatedPrice * assignedProvider.commissionRate / 10000); estimatedPayout = Math.max(serviceProviderAmount, providerLevelAmount);`. V2 dropped the provider-level comparison entirely: auto-dispatch-v2.ts:269-270 `const platformCut = Math.round((estimatedPrice * service.commissionRate) / 10000); estimatedPayout = estimatedPrice - platformCut;`. For the same booking, V1 and V2 surface different estimated payouts to the provider in the assignment notification whenever providerLevelAmount > serviceProviderAmount.
- **Fix:** Extract a single `estimateProviderPayout(provider, service, estimatedPrice)` helper used by both V1 and V2 so the commission math cannot diverge; restore the `Math.max` provider-level path in V2 (or remove it from V1 if intentionally deprecated).
- **Verified:** Confirmed from source. auto-dispatch.ts:165-167 (V1) computes the service-commission-branch payout as Math.max(serviceProviderAmount, providerLevelAmount), comparing the service-level deduction against the provider-level commission rate. auto-dispatch-v2.ts:269-270 (V2) computes only estimatedPrice - platformCut, dropping the provider-level comparison entirely. Both feed the result into the same n…

### Cascade re-dispatch failure (non-exhausted) silently orphans the booking with no admin alert
- **Location:** `server/api/lib/offer-expiry.ts:112`  
- **Dimension:** Dispatch V1/V2 & cascade · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** In the cascade branch, the booking was already reverted to `confirmed`/`providerId=null`/`offerExpiresAt=null` (lines 44-52). If the re-dispatch returns failure for a reason other than exhaustion — e.g. V2 returns `{ success:false, reason:'No providers within range' }` at attempt<MAX (auto-dispatch-v2.ts:205-216) — offer-expiry only does `result.manualNeeded++; logger.error(...)` (lines 113-118) with NO `broadcastToAdmins({ type: 'booking:dispatch_failed' })` (that broadcast exists only on the exhausted branch, lines 83-89). The booking is left `confirmed` with no provider and no pending offer, so no future offer-expiry run will ever pick it up again (it requires `status='dispatched'` + non-null offerExpiresAt), and admins are never notified. The job is stranded silently until a human notices.
- **Fix:** On a non-exhausted cascade failure, emit the same admin `booking:dispatch_failed`/needs-manual broadcast as the exhausted path, and/or leave the booking in a state a retry mechanism can re-pick (e.g. keep it eligible for the pending-dispatch cron).
- **Verified:** Confirmed in code. offer-expiry.ts:44-52 unconditionally reverts the booking to confirmed/providerId=null/offerExpiresAt=null before cascading. In the cascade branch (lines 94-119), if autoDispatchBookingV2 returns success:false for a non-exhausted reason — e.g. auto-dispatch-v2.ts:96-102 ("No available providers") or 205-216 ("No providers within range" at attempt<MAX) — offer-expiry only does ma…

### Duplicate route registrations in provider.ts shadow each other (dead, divergent handlers)
- **Location:** `server/api/routes/provider.ts:1192`  
- **Dimension:** Large-file maintainability · **Category:** bug · **Reviewer confidence:** high
- **Evidence:** `app.get("/earnings/summary")` is defined twice (line 555 and line 1192) and `app.get("/earnings/history")` twice (line 658 and line 1231) on the same Hono instance. Hono runs the first matching handler and returns its Response, so the second copies are dead code. The two copies return DIFFERENT shapes: the line-555 summary returns `{totalEarned, pendingPayout, paidOut, thisMonthEarnings, ...}` while the line-1192 copy returns `{thisWeek, thisMonth, allTime, pending, held, clawbacks, totalJobs}` (the only one exposing `held`/`clawbacks`). The line-658 history returns `{data, total, page, ...}` with `commissionDeducted`, while line-1231 returns `{payouts, pagination}`. A maintainer editing the wrong copy gets silently no effect.
- **Fix:** Delete or rename the shadowed second definitions (lines 1189-1276). Decide which response shape the client actually consumes; if both are needed give them distinct paths (e.g. `/earnings/summary/v2`). After removing duplicates, the `/earnings/pending` (line 1278) and quote endpoints can move into a dedicated `earnings.ts` sub-router given the size.
- **Verified:** Confirmed from the code. server/api/routes/provider.ts registers app.get("/earnings/summary") twice (line 555 and 1192) and app.get("/earnings/history") twice (line 658 and 1231) on the same Hono `app` instance. Hono runs the first registered matching handler and returns once it produces a Response, so the line-1192/1231 copies are dead code. The shapes genuinely diverge: line 555 returns {totalEa…

### all-steps-complete auto-transition reimplemented inline instead of using the existing shared helper
- **Location:** `server/api/routes/admin-providers.ts:1040`  
- **Dimension:** Large-file maintainability · **Category:** maintainability · **Reviewer confidence:** high
- **Evidence:** A shared helper `checkAllStepsCompleteAndTransition` exists in server/api/lib/all-steps-complete.ts and is used in onboarding.ts (lines 865, 1310, 1608). But admin-providers.ts never imports it and hand-rolls the same 'fetch all steps → every() complete (treating current stepId as complete) → guarded UPDATE onboarding→pending_review → audit/broadcast/notify' logic TWICE: at lines 1040-1070 (step review) and again at lines 1217-1255 (document approval). onboarding.ts's dashboard GET also reinlines a third variant at lines 69-112. These copies have already drifted — the admin-providers copies omit the `migrationBypassExpiresAt` migration-completion branch the helper handles, so a migrating provider approved via admin won't have its bypass cleared.
- **Fix:** Import and call `checkAllStepsCompleteAndTransition(id, completedStepId, trigger, {userId, ipAddress, userAgent}, provider)` at the two admin-providers.ts sites and in the onboarding.ts dashboard GET, deleting the inline blocks. This also fixes the migration-branch drift for free.
- **Verified:** Confirmed from source. The shared helper checkAllStepsCompleteAndTransition exists in server/api/lib/all-steps-complete.ts and is imported/used in onboarding.ts (lines 21, 865, 1310, 1608). admin-providers.ts never imports it (grep for "all-steps-complete" matches only a comment at line 1216) and reinlines the all-steps-complete auto-transition logic twice: step review at lines 1040-1070 and docum…

### POST /apply and POST /invite-accept are ~120-line near-duplicates
- **Location:** `server/api/routes/onboarding.ts:282`  
- **Dimension:** Large-file maintainability · **Category:** maintainability · **Reviewer confidence:** high
- **Evidence:** `/apply` (282-407) and `/invite-accept` (410-569) share an almost identical body: existing-user check, existing-provider check, `bcrypt.hash(password,10)`, a transaction inserting users+providers then calling `initializeOnboardingPipeline`, the same unique-violation catch, the FCRA-consent `logAudit`, the bgStep `onboarding.step_started` audit, the post-tx `initiateCheckrBackgroundCheck(...).catch()`, `notifyApplicationReceived(...)`, and the identical 201 response mapping `s.stepType === "background_check" ? {...s, status:"in_progress"} : s`. The only differences are name/email source (form body vs invite token) and marking the invite accepted.
- **Fix:** Extract `createProviderAndStartOnboarding({name, email, phone, password, serviceArea, specialties, dob?}, tx-extras)` returning `{user, provider, steps, bgStep}` plus a shared `finalizeRegistration(result, {dob, inviteToken?}, reqInfo)` that does the FCRA/bgStep audits, Checkr init, notify, and response mapping. Each route becomes ~15 lines of source-specific setup.
- **Verified:** Confirmed from /root/road-side-atl/server/api/routes/onboarding.ts. POST /apply (lines 282-407) and POST /invite-accept (lines 410-569) are genuine ~120-line near-duplicates. Both share, verbatim or near-verbatim: existing-user check, existing-provider check, bcrypt.hash(password,10), the same db.transaction inserting users+providers then calling initializeOnboardingPipeline, the identical err.mes…

### onboarding.ts (1619 lines) mixes four unrelated concerns in one router
- **Location:** `server/api/routes/onboarding.ts:29`  
- **Dimension:** Large-file maintainability · **Category:** maintainability · **Reviewer confidence:** high
- **Evidence:** One Hono app spans cohesive but unrelated groups: registration/lifecycle (`/dashboard`, `/apply`, `/invite-accept`, `/reapply` — lines 34-672), step/training (`/steps/:stepId`, `/training`, `/training/acknowledge/:cardId`, `/background-check/retry` — 674-924), document upload (`/upload-url`, `/documents*`, `/steps/:stepId/submit` — 926-1138), and Stripe Connect + Identity + IC-agreement (`/stripe-link`, `/stripe-status`, `/stripe-dashboard`, `/identity/*`, `/ic-agreement*` — 1140-1617). Each group has its own imports (Checkr, S3, stripe, training-content, ic-agreement-content) and no cross-references except the shared `provider = db.query.providers.findFirst({where: eq(providers.userId, user.id)})` lookup.
- **Fix:** Split into `onboarding/registration.ts`, `onboarding/steps.ts`, `onboarding/documents.ts`, `onboarding/stripe.ts` sub-routers, each `app.route()`-mounted under the same base, keeping `initializeOnboardingPipeline`/`splitName`/`initiateCheckrBackgroundCheck` in a shared `onboarding/lib.ts`.
- **Verified:** Confirmed from the actual code. /root/road-side-atl/server/api/routes/onboarding.ts is 1619 lines with a single `const app = new Hono()` (line 29). It mounts ~17 routes spanning four cohesive-but-unrelated concern clusters at the claimed line ranges: registration/lifecycle (/dashboard:34, /apply:282, /invite-accept:410, /reapply:574), steps/training (/steps/:stepId:675, /training:736, /training/ac…

### Payment-confirmation side-effects duplicated across two admin endpoints
- **Location:** `server/api/routes/admin.ts:469`  
- **Dimension:** Large-file maintainability · **Category:** maintainability · **Reviewer confidence:** high
- **Evidence:** `PATCH /payments/:id/confirm` (469-570) and `POST /bookings/:id/confirm-payment` (573-678) both run the same post-confirmation pipeline: `logAudit('payment.confirm')`, `incrementCleanTransaction(userId)`, fetch service via `db.query.services.findFirst`, fetch provider name, fire-and-forget `notifyPaymentConfirmed(...)`, `logAudit('payment.receipt_sent')`, and `createPayoutIfEligible(bookingId)`. The logic is copy-pasted with subtle divergence (the PATCH path guards on `!wasAlreadyConfirmed`, the POST path guards via a pre-insert `alreadyConfirmed` 409 check), which is exactly the kind of drift that causes double-receipts/double-payouts.
- **Fix:** Extract `finalizePaymentConfirmation(payment, booking, method, adminUserId, reqInfo)` containing the audit + trust-tier + receipt + payout block, and call it from both handlers after each has resolved its own payment row/idempotency.
- **Verified:** Confirmed from server/api/routes/admin.ts. PATCH /payments/:id/confirm (470-570) and POST /bookings/:id/confirm-payment (573-678) both run the same post-confirmation pipeline: logAudit('payment.confirm'), incrementCleanTransaction, service fetch via db.query.services.findFirst, provider name lookup, fire-and-forget notifyPaymentConfirmed(...), logAudit('payment.receipt_sent'), and createPayoutIfEl…

### SMS send failures are silently swallowed — no log, no retry, no alert
- **Location:** `lib/notifications/sms.ts:74`  
- **Dimension:** Notifications & outbound webhooks · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** sendSMS swallows every Twilio error with a bare catch and no logging: `} catch {\n    return { success: false };\n  }` (lib/notifications/sms.ts:74-76). The not-configured and rate-limited paths also return `{ success: false }` with no log (lines 63-64). Callers throughout lib/notifications/index.ts invoke this inside `Promise.allSettled([...])` and never inspect the returned `success` flag, so a failed transactional SMS (payout sent, account suspended, booking dispatched) produces zero signal. This is the SMS counterpart to the email hardening done in PR #135 (sendEmail logs via `logger.error` at email.ts:32) — SMS was never hardened.
- **Fix:** Log failures in the catch block (`logger.error('[SMS] send failed', { phone, error })`) and on the not-configured/rate-limited early returns, and return the error reason so callers can surface/alert on transactional-SMS loss. Consider an alert (Slack/ops) for high-value notifications like payout/suspension.
- **Verified:** Confirmed from source. lib/notifications/sms.ts:74-76 swallows all Twilio errors in a bare `} catch { return { success: false }; }` with no logging; the not-configured (line 63) and rate-limited (line 64) early returns also return { success: false } silently. sms.ts has no logger import at all, and the return type lacks any error field so callers can't surface a reason. The email counterpart (emai…

### In-memory per-process SMS rate limiter silently drops legitimate transactional SMS
- **Location:** `lib/notifications/sms.ts:64`  
- **Dimension:** Notifications & outbound webhooks · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** `isRateLimited` (lib/notifications/sms.ts:40-55) keys a single `Map` by phone with a 60s window; `sendSMS` returns `{ success: false }` and sends nothing when triggered (line 64). Two distinct transactional messages to the same customer within 60s (e.g. `notifyBookingCreated` confirmation SMS followed shortly by `notifyStatusChange` → 'dispatched' to the same `contactPhone`) cause the second to be silently dropped with no log. The Map is also per-process, so it is both lossy (drops wanted messages) and ineffective across multiple instances (no cross-instance limiting).
- **Fix:** Exempt distinct transactional message types from the throttle (or key by phone+messageType), move limiting to a shared store (Redis) for multi-instance correctness, and at minimum log when a message is dropped by the limiter so silent loss is observable.
- **Verified:** Confirmed in lib/notifications/sms.ts. The rate limiter (lines 41-55) uses a single in-memory Map keyed only by phone with a 60s window, and sendSMS (line 64) returns {success:false} sending nothing and logging nothing when triggered. Real call paths reproduce the silent drop: notifyBookingCreated (index.ts:42) sends a confirmation SMS to contactPhone, and a subsequent notifyStatusChange to the sa…

### SSRF: partner webhook URL not restricted to public hosts; cron POSTs to arbitrary URLs
- **Location:** `server/api/lib/outbound-webhooks.ts:71`  
- **Dimension:** Notifications & outbound webhooks · **Category:** security · **Reviewer confidence:** medium
- **Evidence:** Subscription URL is validated only by `url: z.url("Valid https URL required")` (lib/validators.ts:529), which accepts any URL including `http://` and private/link-local hosts despite the message — there is no host/protocol allowlist. The cron worker then blindly fetches it: `await fetch(sub.url, { method: 'POST', ... })` (outbound-webhooks.ts:71). An authenticated B2B owner can register `http://169.254.169.254/latest/meta-data/` or `http://localhost:8000/...`; the worker will issue server-side requests to internal targets (blind SSRF / internal port probing via status+timing).
- **Fix:** At registration enforce https-only and reject private, loopback, link-local, and metadata IP ranges (resolve DNS and re-check before each delivery to prevent rebinding), or route deliveries through an egress proxy with an allowlist.
- **Verified:** Confirmed from code. lib/validators.ts:529 uses Zod v4 (4.3.6 in node_modules) `z.url("Valid https URL required")`, which only checks URL parseability — it accepts http:// and private/loopback/link-local/metadata hosts; the message is cosmetic, not an enforced https/host allowlist. The registration handler server/api/routes/b2b-portal.ts:349-362 (POST /webhooks, B2B owner) stores parsed.data.url v…

### Template email senders swallow Resend failures with no logging
- **Location:** `lib/notifications/email.ts:52`  
- **Dimension:** Notifications & outbound webhooks · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** Only the generic `sendEmail` was hardened with try/catch + `logger.error` (email.ts:22-34). The template-specific senders — `sendBookingConfirmation` (email.ts:52-74), `sendProviderAssignment`, `sendStatusUpdate`, `sendPaymentReceiptEmail`, `sendB2bInvoiceEmail`, etc. — call `await resend.emails.send(...)` with no try/catch. When Resend throws, the rejection is absorbed by the `Promise.allSettled([...])` wrappers in index.ts (e.g. lines 40-47, 401-413) with zero logging, so a failed booking confirmation or invoice email is completely invisible (no log, no retry, no record).
- **Fix:** Wrap each template send in try/catch logging which template+recipient failed (mirroring sendEmail), or route them all through the hardened `sendEmail` and have callers inspect the returned `{ success }` to log/alert on failures.
- **Verified:** Verified against actual code. lib/notifications/email.ts template senders (sendBookingConfirmation:52, sendProviderAssignment:76, sendStatusUpdate:102, sendB2bInvoiceEmail:285, sendPaymentReceiptEmail:360, and the rest) call `await resend.emails.send(...)` with no try/catch and no logging, unlike the generic sendEmail (lines 22-34) which is wrapped in try/catch + logger.error and returns {success}…

### A rejected document permanently blocks step auto-completion
- **Location:** `server/api/routes/admin-providers.ts:1190`  
- **Dimension:** Onboarding state machine · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** On document approval the all-approved gate is `const allApproved = stepDocs.every((d) => d.id === documentId ? true : d.status === "approved")` over ALL docs for the step. Document rejection (admin-providers.ts:1131) leaves the old row with status "rejected" and never changes the onboarding step. When a provider re-uploads after rejection, POST /documents inserts a NEW row (onboarding.ts:999-1008) but the prior rejected row remains. The rejected row's status is "rejected" ≠ "approved", so `every(... === "approved")` is permanently false — the step can never reach "complete" (admin-providers.ts:1197-1204) even after the replacement document is approved. The provider is stuck in_progress with no path to finish onboarding.
- **Fix:** Scope the all-approved check to non-rejected documents (e.g. ignore status==="rejected" rows, or only consider the latest doc per documentType), or supersede/delete the rejected row on resubmission so it no longer participates in the gate.
- **Verified:** Confirmed from code. admin-providers.ts:1184-1192 fetches stepDocs for the step/provider with NO status filter, then gates auto-completion on `stepDocs.every((d) => d.id === documentId ? true : d.status === "approved")`. The rejection path (admin-providers.ts:1109-1146) only updates the document row's status to "rejected" and leaves it in place; the step is untouched. On resubmit, onboarding.ts:97…

### Checkr status mapping: adjudication precedence over status can miss completion
- **Location:** `server/api/routes/webhooks.ts:931`  
- **Dimension:** Onboarding state machine · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** `const effectiveStatus = adjudication || checkrStatus;` then `const newStepStatus = CHECKR_STATUS_MAP[effectiveStatus] || "in_progress";`. CHECKR_STATUS_MAP (lib/constants.ts:182-189) has no entry for "engaged" or "pre_adverse_action", both valid CheckrReport.adjudication values (lib/checkr.ts:31). Because adjudication takes precedence over status, a passing report (status "clear") that also carries adjudication "engaged" yields effectiveStatus="engaged" → unmapped → fallback "in_progress", so a cleared candidate is NOT marked complete and never auto-promotes. The map also omits report status "dispute" (lib/checkr.ts:30), silently falling back to in_progress.
- **Fix:** Prefer a terminal checkrStatus over a non-terminal adjudication, or add explicit map entries (engaged→complete/clear handling, pre_adverse_action→pending_review, dispute→pending_review). Only let adjudication override when it is an adverse value.
- **Verified:** Confirmed from actual code. webhooks.ts:931-932 computes `effectiveStatus = adjudication || checkrStatus` then `CHECKR_STATUS_MAP[effectiveStatus] || "in_progress"`. CHECKR_STATUS_MAP (lib/constants.ts:182-189) only contains clear/consider/suspended/adverse_action/post_adverse_action and explicitly omits non-terminal values. The CheckrReport type (server/api/lib/checkr.ts:30-31) confirms `adjudica…

### Provider can be auto-promoted to pending_review before identity_verification when STRIPE_IDENTITY is enabled
- **Location:** `server/api/lib/all-steps-complete.ts:76`  
- **Dimension:** Onboarding state machine · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** ONBOARDING_STEP_TYPES (lib/constants.ts:119-121) lists only 6 steps and does NOT include identity_verification; that step is created lazily only when the provider calls POST /identity/start (onboarding.ts:1395-1405). checkAllStepsCompleteAndTransition computes completion over only the rows that exist: `const allComplete = allSteps.every((s) => s.id === completedStepId ? true : s.status === "complete")` (all-steps-complete.ts:80-82). So when STRIPE_IDENTITY is enabled but the provider has not yet started identity verification, completing the 6 base steps promotes the provider to pending_review with no identity_verification row in existence — bypassing the gate. Relatedly, POST /reapply resets all steps to pending (onboarding.ts:610-622) but never re-initializes a previously-created identity_verification step, leaving it stuck at "pending" and blocking re-promotion.
- **Fix:** If identity verification is required, include identity_verification in the initial step set (gated on the feature flag) so the all-steps gate accounts for it, or have the all-steps check explicitly require a completed identity step when STRIPE_IDENTITY is enabled.
- **Verified:** Confirmed from code. ONBOARDING_STEP_TYPES (lib/constants.ts:119-121) contains only the 6 base steps and excludes identity_verification, which is created lazily only on POST /identity/start (onboarding.ts:1387-1405). checkAllStepsCompleteAndTransition (all-steps-complete.ts:76-84) computes completion over only the onboarding_steps rows that exist (db.query.findMany + every), so when STRIPE_IDENTIT…

### Price-override base diverges from charge-time base, breaking B2 invariant for destination charges
- **Location:** `server/api/lib/payout-calculator.ts:152`  
- **Dimension:** Payout & commission correctness · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** Payout uses `const effectivePrice = booking.priceOverrideCents ?? confirmedPayment.amount;` and then `computeProviderAmount(effectivePrice, ...)`. But the Stripe destination-charge application fee is computed at checkout from `booking.estimatedPrice` (payments.ts:159-160: `computeProviderAmount(booking.estimatedPrice, ...)` then `applicationFeeAmount = Math.max(0, booking.estimatedPrice - providerShare)`). For a destination charge the transfer to the provider is fixed/immutable at charge time on `estimatedPrice`. An admin can set `priceOverrideCents` afterward (admin.ts:1130-1140, no payment-status guard). The destination-charge branch (payout-calculator.ts:167-189) then RECORDS `amount: providerAmount` computed on the override price, so the recorded payout no longer equals the money Stripe actually transferred. The file's own docstring claims `the Stripe transfer to the provider always equals the recorded payout (B2)`, which is violated.
- **Fix:** For destination charges, compute the recorded payout from the SAME basis used at charge time (persisted `payment.amount` / the actual captured charge amount and the persisted `payment.applicationFeeAmount`), not from `priceOverrideCents`. Either record `payout.amount = payment.amount - payment.applicationFeeAmount` for destination charges, or block `priceOverrideCents` edits once a destination charge is confirmed.
- **Verified:** Confirmed from code. payout-calculator.ts:152 computes effectivePrice = priceOverrideCents ?? confirmedPayment.amount, then line 161 providerAmount = computeProviderAmount(effectivePrice,...) and the destination branch (167-189) records amount: providerAmount. But for destination charges the Stripe transfer is fixed at charge time on booking.estimatedPrice (payments.ts:159-160, unit_amount=estimat…

### resolveCommissionBp tie-break is non-deterministic (no stable ordering / no ORDER BY)
- **Location:** `server/api/lib/commission.ts:37`  
- **Dimension:** Payout & commission correctness · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** The query `db.select().from(commissionRules).where(and(eq(active,true), or(...scopeConds)))` has no ORDER BY, and the in-memory sort `(a, b) => (SCOPE_RANK[b.scope] - SCOPE_RANK[a.scope]) || b.priority - a.priority` has no final tie-breaker. If two active rules share the same scope AND the same priority (e.g. two account-scoped rules), the winner depends on the DB's unspecified row order. The checkout call (payments.ts:154) and the payout call (payout-calculator.ts:156) resolve independently; if they pick different rules the application fee won't match the recorded payout, again breaking the B2 invariant the module is built around.
- **Fix:** Add a deterministic final sort key (e.g. `|| a.id.localeCompare(b.id)` or `|| a.createdAt - b.createdAt`) and/or a corresponding ORDER BY in the query, and ideally enforce a uniqueness constraint on (scope, scopeId, active) so overlapping rules can't exist.
- **Verified:** Confirmed in code. commission.ts:31-34 has no ORDER BY, and the sort at line 37-39 ((SCOPE_RANK diff) || (priority diff)) has no final tie-breaker. The schema (db/schema/commission-rules.ts) gives priority a default of 0 and has NO unique constraint on (scope, scopeId, active); the POST route (routes/commission-rules.ts:21-50) enforces no uniqueness, so two active rules with identical scope+scopeI…

### Negotiated-rate heuristic mislabels a provider deliberately set to the default rate
- **Location:** `server/api/lib/payout-calculator.ts:38`  
- **Dimension:** Payout & commission correctness · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** `const negotiated = provider.rateIsNegotiated ?? provider.commissionRate !== DEFAULT_PROVIDER_COMMISSION_RATE;` (DEFAULT = 7000). The persisted flag is itself derived from the same heuristic in admin-providers.ts:499 (`updateData.rateIsNegotiated = parsed.data.commissionRate !== 7000`). A provider whose special arrangement happens to be exactly 70% is therefore recorded as NOT negotiated, so the service-level cut (`serviceProviderAmount`) is applied instead of the provider's intended 70% share — the provider-rate override silently loses to the service rate whenever the negotiated rate equals the system default.
- **Fix:** Make `rateIsNegotiated` an explicit, independently-stored boolean set by the admin action (not inferred from value equality), and stop deriving it from `commissionRate !== 7000` in admin-providers.ts; treat the column as authoritative in computeProviderAmount.
- **Verified:** Confirmed in actual code. db/schema/providers.ts:46 defines rateIsNegotiated as a real boolean column whose documented intent (lines 44-46) is to be an authoritative flag marking a deliberate negotiated arrangement. However both admin write paths derive it from value-equality rather than explicit intent: admin-providers.ts:467-468 (create) and admin-providers.ts:499 (update) both set rateIsNegotia…

### bookings table has zero secondary indexes — every hot filter/sort is a seq scan + sort
- **Location:** `db/schema/bookings.ts:45-90`  
- **Dimension:** Performance & DB efficiency · **Category:** performance · **Reviewer confidence:** high
- **Evidence:** The bookings pgTable has no index callback (third arg) at all. Confirmed via `grep CREATE INDEX db/migrations`: only unique-constraint indexes exist, none on bookings. Yet bookings is filtered/sorted on unindexed columns in the busiest paths: admin list `.orderBy(desc(bookings.createdAt))` + `eq(bookings.status, status)` (server/api/routes/admin.ts:139,156); customer list `.where(eq(bookings.userId, user.id)).orderBy(desc(bookings.createdAt))` (server/api/routes/customer.ts:39-40); provider location ping `and(eq(bookings.providerId, provider.id), inArray(bookings.status,[...]))` (server/api/routes/provider.ts:351-353); dashboard sparkline `groupBy(date(bookings.createdAt))` (admin.ts:111-114). This runs on the prod table shared dev+prod.
- **Fix:** Add btree indexes via the schema index callback: createdAt (for ORDER BY DESC), status, userId, serviceId (FK), and a composite (providerId, status). A composite (status, createdAt) best serves the admin list. Generate a drizzle migration.
- **Verified:** Confirmed from code. db/schema/bookings.ts defines pgTable("bookings", {...}) with no third index-callback argument — zero secondary indexes. db/migrations/0000_baseline.sql contains no CREATE INDEX on the bookings table (only uniq_payout_standard_booking on provider_payouts); the FK ALTER TABLE statements add constraints but Postgres does not auto-index referencing FK columns, so userId/serviceId…

### provider_payouts missing indexes on providerId/status — provider dashboard does repeated full scans
- **Location:** `db/schema/provider-payouts.ts:39-49`  
- **Dimension:** Performance & DB efficiency · **Category:** performance · **Reviewer confidence:** high
- **Evidence:** provider_payouts declares only the two unique-constraint indexes; nothing on providerId, status, or createdAt. The provider /stats endpoint fires four aggregations all filtered `eq(providerPayouts.providerId, provider.id)` (server/api/routes/provider.ts:575-576 and the Promise.all at 582-622), the admin payouts list filters `eq(providerPayouts.status, status)` and `eq(providerPayouts.providerId, providerId)` (server/api/routes/admin-payouts.ts:36,40), and the migrate path scans by providerId+status (server/api/lib/payout-calculator.ts:345-351). Every one is a seq scan over the whole payouts table.
- **Fix:** Add btree indexes on providerPayouts.providerId, providerPayouts.status, and a composite (providerId, payoutType, status) matching the dashboard aggregation predicates; createdAt for the list ORDER BY.
- **Verified:** Confirmed in code. db/schema/provider-payouts.ts:33-43 (and matching migration db/migrations/0000_baseline.sql:530-531) define ONLY two partial unique indexes — on bookingId (uniq_payout_standard_booking) and originalPayoutId (uniq_payout_clawback_original). There is no index on providerId, status, or createdAt. Postgres does not auto-index FK columns, so providerId is unindexed. The cited hot-pat…

### payments missing index on bookingId (FK) and status/createdAt — joins and payment-confirm lookups seq scan
- **Location:** `db/schema/payments.ts:25-58`  
- **Dimension:** Performance & DB efficiency · **Category:** performance · **Reviewer confidence:** high
- **Evidence:** payments declares only uniqStripeSession (partial unique on stripeSessionId). bookingId, status and createdAt are unindexed despite being the most-joined/filtered columns: the payment-confirmation hot path looks up `and(eq(payments.bookingId, bookingId), eq(payments.status,'confirmed'))` (server/api/lib/payout-calculator.ts:118-121, also invoice-generator.ts:48-49, admin-payouts.ts:261); admin/bookings leftJoins payments on bookingId (admin.ts:184); sparklines/dashboard filter `and(eq(payments.status,'confirmed'), gte(payments.createdAt, ...))` (admin.ts:83,89,122); financial-reports innerJoins payments→bookings on bookingId 7+ times (financial-reports.ts:126,187,199,...). Without an index on bookingId each join/lookup scans the full payments table.
- **Fix:** Add btree index on payments.bookingId (critical FK), and a composite (status, createdAt) for the dashboard revenue aggregations.
- **Verified:** Confirmed from db/schema/payments.ts:23-53: the only index is uniqStripeSession (partial unique on stripeSessionId). bookingId, status, and createdAt are unindexed, and Postgres does not auto-index FK columns. The cited query hot paths are real (verified via grep): payout-calculator.ts:119-120 (bookingId+status lookup), reconciliation.ts:742-745/801-802, financial-reports.ts (6+ innerJoins on paym…

### Admin payouts list returns ALL payouts with 3 joins and no pagination
- **Location:** `server/api/routes/admin-payouts.ts:44-63`  
- **Dimension:** Performance & DB efficiency · **Category:** performance · **Reviewer confidence:** high
- **Evidence:** GET / builds `db.select({...payout, provider, booking, service}).from(providerPayouts).innerJoin(providers).innerJoin(bookings).innerJoin(services).orderBy(desc(providerPayouts.createdAt))` with no `.limit()`/`.offset()` — only optional status/providerId filters. Unlike admin/bookings (which paginates, admin.ts:152-153) this fetches the entire payout history joined four-wide on every admin payouts page load, growing unbounded with volume.
- **Fix:** Add page/limit query params with limit/offset and a total count (mirror the admin/bookings handler), defaulting status to 'pending' for the operational view.
- **Verified:** Confirmed at admin-payouts.ts:43-65. GET / selects payout+provider+booking+service across 3 innerJoins, orders by desc(createdAt), and awaits the query with NO .limit()/.offset() — only optional status/providerId filters. The sibling admin.ts:145-151 /bookings handler does paginate (page/limit/offset), confirming the asymmetry. So the admin payouts list fetches the entire joined payout history on …

### dispatch_logs missing index on bookingId FK used for per-booking lookups
- **Location:** `db/schema/dispatch-logs.ts:15-33`  
- **Dimension:** Performance & DB efficiency · **Category:** performance · **Reviewer confidence:** high
- **Evidence:** dispatch_logs declares no indexes. It is queried by bookingId on cascade-dispatch paths: `db.query.dispatchLogs.findMany({ where: eq(dispatchLogs.bookingId, bookingId) })` in the provider accept/decline flow (server/api/routes/provider.ts:200-201) and in the offer-expiry cron (server/api/lib/offer-expiry.ts:96-97). Without an index these scan the full dispatch_logs table, which grows with every dispatch attempt (multiple rows per booking).
- **Fix:** Add a btree index on dispatchLogs.bookingId (and optionally assignedProviderId) via the schema index callback.
- **Verified:** Confirmed from code. db/schema/dispatch-logs.ts:8-29 calls pgTable with no third-arg index callback, so dispatch_logs has zero indexes; bookingId (line 12) is only an FK reference (Postgres does not auto-index FKs). It is queried by bookingId via db.query.dispatchLogs.findMany({ where: eq(dispatchLogs.bookingId, ...) }) in server/api/routes/provider.ts:200-202 (provider decline/re-dispatch) and se…

### Provider-scarcity multiplier has no feature flag and silently inflates every price
- **Location:** `server/api/lib/pricing-engine.ts:169-191`  
- **Dimension:** Pricing engine & multipliers · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** Every other pricing dimension is gated and default-off (surge l.154 `isFeatureEnabled(FEATURE_FLAGS.SURGE_PRICING)`, matrix l.196, zone l.214, weather l.235; all four default `false` in feature-flags.ts:62-65). Step 3 scarcity runs unconditionally: `const availableCount = await getAvailableProviderCount(); ... if (availableCount < SCARCITY_THRESHOLD && availableCount >= 0) { const deficit = SCARCITY_THRESHOLD - availableCount; scarcityMultiplierBp = Math.min(MAX_SCARCITY_BP, 10000 + deficit * SCARCITY_STEP_BP); ...}`. There is no SCARCITY entry in FEATURE_FLAGS. getAvailableProviderCount counts providers with isAvailable=true AND status='active'; on a fresh/empty deployment or off-hours that count is 0, giving deficit=3 → multiplier 11500 (1.15x) applied to EVERY quote with no kill-switch.
- **Fix:** Add a FEATURE_FLAGS.SCARCITY_PRICING flag (default false) and wrap step 3 in `if (await isFeatureEnabled(FEATURE_FLAGS.SCARCITY_PRICING))`, matching surge/zone/weather. Also guard availableCount===0 (treat zero providers as 'no data', not max scarcity).
- **Verified:** Confirmed in code. pricing-engine.ts:169-191 runs the scarcity multiplier with no feature-flag gate, while surge (l.154), matrix (l.196), zone (l.214), and weather (l.235) are all wrapped in isFeatureEnabled and default false in feature-flags.ts:62-65. There is no SCARCITY entry in FEATURE_FLAGS (l.34-40). The guard `availableCount < SCARCITY_THRESHOLD && availableCount >= 0` always enters for cou…

### 3x cap clamps finalPrice but breakdown line-items are not adjusted, so they no longer sum to the charge
- **Location:** `server/api/lib/pricing-engine.ts:255-265`  
- **Dimension:** Pricing engine & multipliers · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** Breakdown amounts are accumulated as incremental contributions for each step (e.g. surge l.159 `Math.round((afterTimeBlock*surgeMultiplierBp)/10000)-afterTimeBlock`, scarcity l.183, zone l.222, weather l.243). finalPrice is computed from the full product then clamped: `if (finalPrice > priceCeiling) { finalPrice = priceCeiling; }`. The breakdown array is never re-scaled, so when the cap triggers the customer-facing line items sum to the UNCAPPED total, not the charged finalPrice — the receipt/quote breakdown is internally inconsistent and overstates the components.
- **Fix:** When the ceiling clamps finalPrice, proportionally rescale the breakdown contributions (or append an explicit 'Price cap (-X)' adjustment line) so sum(breakdown.amount) === finalPrice.
- **Verified:** Confirmed in /root/road-side-atl/server/api/lib/pricing-engine.ts. The breakdown array is built from incremental per-step contributions (afterTimeBlock at l.146; surge l.159 = afterSurge−afterTimeBlock; scarcity l.183; matrix l.201; zone l.222; weather l.243), which telescope to the full uncapped product. finalPrice is computed from that same uncapped product (l.255-258) and only afterward clamped…

### Booking creation re-computes price instead of honoring the displayed estimate — surge/scarcity/weather can drift between quote and booking
- **Location:** `server/api/routes/bookings.ts:54-60`  
- **Dimension:** Pricing engine & multipliers · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** The estimate endpoint computes a price (pricing-estimate.ts:40 `calculateBookingPrice(...)`), but booking creation independently re-runs `const pricing = await calculateBookingPrice(data.serviceId, ...)` and stores `pricingSnapshot: { basePrice, multiplier, blockName, estimatedPrice, ... }` (bookings.ts:134-142). No quote token/quoteId locks the previously shown price, and the snapshot records the recomputed value, not the quoted one. Because surge (60-min booking count), scarcity (live provider count), weather and time-block all change with wall-clock/state, the customer can be charged a different amount than the estimate they saw, with no immutability guarantee.
- **Fix:** Issue a signed/persisted quote (id + expiry + frozen finalPrice) from the estimate path and have booking creation validate and consume it, falling back to recompute only when no valid quote is supplied.
- **Verified:** Confirmed from code. pricing-estimate.ts:40-46 returns a computed price without persisting any quote. bookings.ts:54-60 independently re-runs calculateBookingPrice and stores pricingSnapshot (lines 134-142) with the RECOMPUTED finalPrice as estimatedPrice; createBookingSchema accepts no quoteId/token, so no prior price is locked. The bookingQuotes table imported on line 3 is unrelated — it is the …

### Heartbeat never verifies pong — dead connections leak in the connections map and receive broadcasts
- **Location:** `server/websocket/server.ts:44`  
- **Dimension:** WebSocket & push realtime · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** The heartbeat only sends pings: `setInterval(() => { if (ws.readyState === 1) ws.ping(); else clearInterval(pingInterval); }, 30_000)`. There is no `ws.on("pong")`, no `isAlive` flag, and no `ws.terminate()` on a missed pong. On a silent network drop (no TCP FIN/close frame) the 'close' event never fires, so `removeConnection` is never called and the ConnectionInfo stays in the `connections` Map indefinitely (connections.ts:12-17). These zombie entries accumulate (memory leak) and are still iterated by broadcastToAdmins/getAdminConnections on every event. Compounding it, re-sending an `auth` message (different userId) calls addConnection again but the close handler only removes the last userId (server.ts:38-39), orphaning the earlier entry.
- **Fix:** Use the standard ws liveness pattern: set `ws.isAlive = true` on connect and in `ws.on("pong")`; on each interval, if `!ws.isAlive` call `ws.terminate()` (which fires close → removeConnection), otherwise set `isAlive=false` and ping. Also guard against double-auth (ignore auth after userId is set, or removeConnection for the prior userId before re-adding).
- **Verified:** Confirmed from actual code. server.ts:44-52 sends pings only — no ws.on("pong"), no isAlive flag, no ws.terminate() on a missed pong. removeConnection (connections.ts:19-31) is only called from the close handler (server.ts:36-41), so on a silent half-open drop (no TCP FIN/close frame) close never fires and the ConnectionInfo leaks permanently in the connections Map. Stale entries are still iterate…

### reconcilePaymentsAndPayouts runs unbounded full-table scans with N+1 subqueries daily
- **Location:** `server/api/lib/reconciliation.ts:735`  
- **Dimension:** Reconciliation scheduled jobs · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** The daily job selects ALL historical completed/confirmed payments with no date window or LIMIT (`confirmedPayments`), then issues a per-row `findFirst` for the matching payout (lines 758-764). It then selects ALL standard payouts (`allStandardPayouts`, line 793-796) and issues another per-row `findFirst` against payments (lines 799-804). Both result sets grow monotonically with business volume, and each spawns one query per row (N+1). There is no chunking/`BATCH_SIZE` pacing here (unlike the other loops). As payment/payout history grows this scan becomes O(N) queries over ever-larger tables, eventually exhausting DB connections or timing out the run.
- **Fix:** Restrict the scan to a recent window (e.g. bookings completed in the last N days, or only payouts created since the last successful reconciliation watermark), and replace the N+1 `findFirst` calls with set-based LEFT JOINs (payments LEFT JOIN providerPayouts ON bookingId) so discrepancies are found in one or two queries instead of thousands.
- **Verified:** Confirmed in code. reconcilePaymentsAndPayouts (server/api/lib/reconciliation.ts:735) runs daily (server/cron.ts:107-113, 24*HOUR). It selects ALL confirmed/completed stripe payments with no date window/LIMIT (735-750), then issues a per-row findFirst per payment (758-764) = N+1. It then selects ALL standard payouts (793-796) and issues a per-row findFirst per payout (798-804) = a second N+1. Neit…

### enforceStripeConnectDeadline suspends providers without honoring migrationBypassExpiresAt
- **Location:** `server/api/lib/reconciliation.ts:406`  
- **Dimension:** Reconciliation scheduled jobs · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** The Connect deadline query suspends any provider whose `stripe_connect` step is pending/draft/in_progress and older than 30 days:
```
.where(and(eq(onboardingSteps.stepType, "stripe_connect"), sql`${onboardingSteps.status} IN ('pending','draft','in_progress')`, lt(onboardingSteps.createdAt, deadlineCutoff), sql`${providers.status} NOT IN ('suspended','rejected')`))
```
Unlike `enforceMigrationDeadline` (line 584, which checks `migrationBypassExpiresAt IS NULL OR ... < now`) and `checkMigrationReminders` (line 522), this function never consults `migrationBypassExpiresAt`. A provider explicitly granted a migration bypass who has already started Connect (in_progress step) is still suspended at the 30-day step age, contradicting the bypass grant and the migration grace-period timeline.
- **Fix:** Add the same bypass guard used elsewhere to the `overdue` query: exclude providers where `migrationBypassExpiresAt > now`, and reconcile the fixed 30-day STRIPE_CONNECT_DEADLINE_DAYS against the migration deprecation/grace window so the two deadline jobs cannot suspend a provider the migration policy still considers compliant.
- **Verified:** Confirmed from code. In server/api/lib/reconciliation.ts, enforceStripeConnectDeadline (query lines 406-420) suspends any provider with a pending/draft/in_progress stripe_connect step older than 30 days where status NOT IN ('suspended','rejected'), with NO migrationBypassExpiresAt check. Its two sibling jobs DO honor the bypass: checkMigrationReminders (line 522: skip if migrationBypassExpiresAt >…

### reconcilePaymentsAndPayouts only flags over-payment, missing under-payments and wrong commission
- **Location:** `server/api/lib/reconciliation.ts:778`  
- **Dimension:** Reconciliation scheduled jobs · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** The only amount check is:
```
if (payout.amount > payment.amount) { mismatches++; ... } else { matched++; }
```
It treats any payout <= payment as 'matched' and never validates the expected payout = payment - commission (commission is in basis points). A payout that is far too LOW (provider underpaid), or computed with the wrong commission rate, passes silently as a successful match, giving false confidence that money movement is correct. The reconciliation only catches the extreme/rare case where a payout exceeds the gross payment.
- **Fix:** Compute the expected provider share from the payment amount and the applicable commission basis points and flag a mismatch when `payout.amount` deviates from the expected value beyond a tolerance (e.g. rounding), rather than only when payout exceeds payment.
- **Verified:** Confirmed in server/api/lib/reconciliation.ts:777-789. The sole amount check is `if (payout.amount > payment.amount) { mismatches++ } else { matched++ }`. No commission is computed despite the docstring (line 725) claiming it accounts for commission — grep confirms "commission" appears only in comments in this file. Commission rates exist in basis points on providers (lib/constants.ts:92 DEFAULT_C…

### ENCRYPTION_KEY (PII-at-rest key) is optional and never required/placeholder-checked in production
- **Location:** `lib/env.ts:30`  
- **Dimension:** Secrets, rate-limiting, PII · **Category:** security · **Reviewer confidence:** high
- **Evidence:** `ENCRYPTION_KEY: z.string().regex(/^[0-9a-f]{64}$/i, ...).optional()`. The production fail-closed block (lib/env.ts:78-96) checks only STRIPE/RESEND/GOOGLE placeholders and never validates ENCRYPTION_KEY at all. Yet this key encrypts provider SSN/EIN tax IDs: `const encrypted = encrypt(parsed.data.taxId)` (server/api/routes/admin-providers.ts:361) and decrypt at lines 88/318. With APP_ENV=production the app boots fine with no ENCRYPTION_KEY, then encrypt() throws at runtime (`ENCRYPTION_KEY environment variable required`, server/api/lib/encryption.ts:8) only when an admin saves a tax ID — i.e. PII-at-rest protection is not guaranteed to be configured before serving.
- **Fix:** In lib/env.ts make ENCRYPTION_KEY required (non-optional, same 64-hex regex) when isProductionDeployment, failing closed at startup like the other secrets, instead of deferring the failure to an admin write path.
- **Verified:** Verified against the actual code. lib/env.ts:30 declares `ENCRYPTION_KEY: z.string().regex(/^[0-9a-f]{64}$/i, ...).optional()` — optional. The production fail-closed block (lib/env.ts:78-96) only checks STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, RESEND_API_KEY, AUTH_GOOGLE_ID, AUTH_GOOGLE_SECRET placeholders and never references ENCRYPTION_KEY, so APP_ENV=production boots with no key. encryption.ts…

### In-memory limiter cleanup uses a hardcoded 60s TTL, prematurely evicting longer-window (auth) buckets
- **Location:** `server/api/lib/rate-limiter.ts:17`  
- **Dimension:** Secrets, rate-limiting, PII · **Category:** security · **Reviewer confidence:** high
- **Evidence:** Cleanup deletes any entry where `now - entry.windowStart > 60000`, ignoring config.windowMs. The auth preset is `auth: { maxRequests: 10, windowMs: 300000 }` (5 min) and is applied as in-memory `rateLimitAuth` to the entire public provider-registration surface (`app.use("/*", rateLimitAuth)`, server/api/routes/provider-registration.ts:23). An auth bucket created early in its 5-min window is purged at a cleanup tick once it exceeds 60s of age, resetting the counter before the window expires and handing the attacker a fresh quota — weakening the brute-force lockout.
- **Fix:** Compute the cleanup threshold per entry from the config/window that created it (store windowMs on the entry, or track a max window), e.g. delete only when `now - windowStart > entryWindowMs`; or scope cleanup to the actual window size.
- **Verified:** Confirmed in code. server/api/lib/rate-limiter.ts:17 evicts any bucket older than a hardcoded 60000ms in the periodic cleanup, ignoring config.windowMs. The RateLimitEntry (lines 6-9) only stores count+windowStart, so cleanup cannot know the bucket's window. The auth preset is windowMs=300000 (5 min, line 86), wired as in-memory rateLimitAuth (middleware/rate-limit.ts:157) and applied to the whole…

### Paid Google Maps geocoding is guarded only by a per-process, IP-keyed in-memory limit
- **Location:** `server/api/routes/geocoding.ts:9`  
- **Dimension:** Secrets, rate-limiting, PII · **Category:** security · **Reviewer confidence:** high
- **Evidence:** `app.post("/address", rateLimitStrict, ...)` is the only protection on an unauthenticated endpoint that calls the billable Google Geocoding API (lib/geocoding.ts:13-18). `rateLimitStrict` is the in-memory limiter (store is a process-local Map, server/api/lib/rate-limiter.ts:11) keyed `identifier:endpoint` where identifier is the client IP. It resets on every deploy/restart, is not shared across instances (so the effective ceiling is N_instances * 20/min), and the IP key is trivially rotated. The inline comment claims 'an anonymous loop can't run up the paid Google Maps bill' but the chosen backend does not reliably enforce that.
- **Fix:** Use the durable Postgres-backed limiter (checkRateLimitDb / a rateLimitStrictDb-style middleware) for this paid, unauthenticated endpoint, and consider a global per-minute cap independent of source IP.
- **Verified:** Confirmed from code. geocoding.ts:9 guards the unauthenticated POST /address with only rateLimitStrict, and that handler (line 17) calls geocodeAddress (lib/geocoding.ts:8-18) which invokes the billable Google Geocoding API. rateLimitStrict (middleware/rate-limit.ts:156) uses the in-memory checkRateLimit whose store is a process-local Map (lib/rate-limiter.ts:11) keyed identifier:endpoint with ide…

### account.updated only handles enablement, never reacts to a Connect account becoming restricted/disabled
- **Location:** `server/api/routes/webhooks.ts:534`  
- **Dimension:** Stripe webhooks & idempotency · **Category:** reliability · **Reviewer confidence:** medium
- **Evidence:** The handler bails immediately when charges are not enabled: `if (!account.charges_enabled) break; // Only care about enabled`. There is no branch for a previously-enabled provider whose account later loses charges_enabled (compliance hold, missing KYC, payouts disabled). The provider's onboarding step stays 'complete' and checkout continues routing destination charges to that account (payments.ts useDestinationCharge depends only on provider.stripeConnectAccountId being set), so transfers/charges can fail at charge time with no upstream guard.
- **Fix:** On account.updated with charges_enabled=false (or payouts_enabled=false / requirements.disabled_reason set) for a provider with a stored stripeConnectAccountId, flag the provider (e.g. suspend Connect routing / mark step needing action and alert admins) so checkout stops issuing destination charges to a disabled account.
- **Verified:** Confirmed from code. webhooks.ts:533 `if (!account.charges_enabled) break;` means the account.updated handler only ever transitions the stripe_connect step forward to 'complete' (lines 549-608); there is no branch handling a previously-enabled account losing charges_enabled/payouts_enabled. The checkout routing guard payments.ts:147 (`useDestinationCharge = destinationChargesEnabled && !!provider?…

### reconcilePaymentsAndPayouts (orphan-payment / orphan-payout / amount-mismatch detector) is untested
- **Location:** `server/api/lib/reconciliation.ts:727`  
- **Dimension:** Test coverage gaps · **Category:** test-gap · **Reviewer confidence:** high
- **Evidence:** No test imports reconcilePaymentsAndPayouts (grep returns empty). This is the financial-integrity sweep that flags confirmed payments with no payout, payouts with no confirmed payment, and amount drift. Its mismatch rule only catches over-payment: 
  if (payout.amount > payment.amount) { mismatches++; ... }
It never validates the payout against the EXPECTED commission, so an under-payout or wrong-commission payout (e.g. provider paid 100% because service.commissionRate was 0) is silently counted as matched++.
- **Fix:** Add tests seeding: a confirmed completed-booking payment with no standard payout (expect orphanPayments=1), a standard payout with no confirmed payment (expect orphanPayouts=1), and a payout exceeding its payment (expect mismatches=1). Add a case asserting it should also flag payout != payment - round(payment*commissionRate/10000); this exposes that the current job cannot detect commission-calculation bugs.
- **Verified:** Confirmed from source. reconcilePaymentsAndPayouts (server/api/lib/reconciliation.ts:727) has zero test coverage — no file under tests/ imports it (grep returns empty), whereas sibling reconcile functions are tested. It is live in production via server/cron.ts:111 and server/api/routes/financial-reports.ts:348. The mismatch rule at line 778 is only `if (payout.amount > payment.amount)`, with no ex…

### processPendingPayouts performs real Stripe transfers but has no test guarding idempotency/state flip
- **Location:** `server/api/lib/reconciliation.ts:638`  
- **Dimension:** Test coverage gaps · **Category:** test-gap · **Reviewer confidence:** high
- **Evidence:** No test references processPendingPayouts. It moves money: stripe.transfers.create({ amount: payout.amount, destination: provider.stripeConnectAccountId, ... }, { idempotencyKey: `payout-${payout.id}` }) then flips status to 'paid'. Re-run safety depends entirely on (a) the WHERE status='pending' filter and (b) the idempotencyKey string. A regression that changes the key or skips the status flip on a transfer-succeeded/db-failed path would double-pay providers on the next daily run.
- **Fix:** Add tests with a mocked Stripe asserting: (1) successful transfer flips status to 'paid', records stripeTransferId, and uses idempotencyKey exactly `payout-<id>`; (2) provider without stripeConnectAccountId is skipped (left pending); (3) Stripe throw leaves status='pending' and increments failed, not paid. Locks in the no-double-payout contract.
- **Verified:** Confirmed from code. server/api/lib/reconciliation.ts:638 defines processPendingPayouts, which calls stripe.transfers.create (line 680) with idempotencyKey `payout-${payout.id}` (line 688) and then flips providerPayouts.status to "paid" with stripeTransferId (lines 691-699). No-double-pay safety rests on the WHERE status='pending'/payoutMethod='stripe_connect' filter (lines 655-660) and the idempo…

### Outbound partner webhook delivery worker (HMAC sign + claim-dedup + backoff/dead-letter) is untested
- **Location:** `server/api/lib/outbound-webhooks.ts:44`  
- **Dimension:** Test coverage gaps · **Category:** test-gap · **Reviewer confidence:** high
- **Evidence:** No test references deliverPendingWebhooks/emitPartnerEvent/signPayload. The worker's anti-double-send is a compare-and-swap claim:
  const [claimed] = await db.update(webhookDeliveries).set({ attempts: d.attempts + 1 }).where(and(eq(id, d.id), eq(attempts, d.attempts))).returning();
  if (!claimed) continue;
plus exponential backoff (BACKOFF_BASE_MS * 2 ** (attempts-1)) and dead-letter at MAX_ATTEMPTS=6, and HMAC-SHA256 signing. None of this concurrency/retry logic is exercised.
- **Fix:** Add tests asserting: (1) signPayload produces a stable known HMAC for a fixed secret/body (downstream partners verify this); (2) a 2xx response sets status='delivered'; (3) a non-2xx below MAX_ATTEMPTS reschedules nextAttemptAt with the doubling backoff and keeps status pending; (4) the 6th failure sets status='failed'; (5) a delivery whose attempts changed concurrently is skipped (claim CAS). Catches signature-format breakage and double-delivery regressions.
- **Verified:** Confirmed real. /root/road-side-atl/server/api/lib/outbound-webhooks.ts implements signPayload (HMAC-SHA256, L18-20), emitPartnerEvent (L23-41), and deliverPendingWebhooks (L44-99) with the claim compare-and-swap (L54-59), exponential backoff BACKOFF_BASE_MS*2**(attempts-1) (L93), and dead-letter at MAX_ATTEMPTS=6 (L15,90-91). The worker is wired into production via server/cron.ts:142-143. Grep ov…

### WebSocket broadcast/connection registry untested — role isolation, dead-socket guard, and provider-key footgun
- **Location:** `server/websocket/connections.ts:53`  
- **Dimension:** Test coverage gaps · **Category:** test-gap · **Reviewer confidence:** high
- **Evidence:** No test imports connections.ts. Connections are stored keyed by userId (addConnection, line 12-16), but broadcastToProvider(providerId) does connections.get(providerId) — it relies on every caller passing provider.userId, not the providerId. A caller that passes a real provider.id silently no-ops (provider never receives the job offer). broadcastToAdmins filters role==="admin" and sendToConnection only sends when ws.readyState===1; neither the role filter nor the readyState guard is tested.
- **Fix:** Add unit tests with a fake ws ({ readyState, send }) asserting: (1) broadcastToUser/broadcastToProvider deliver only to the connection registered under that exact key and not to other users (cross-user leak guard); (2) broadcastToAdmins reaches only role='admin' connections; (3) sendToConnection is a no-op when readyState!==1; (4) removeConnection cleans up the user entry when its set empties. Catches role leakage, sends to closed sockets, and the providerId-vs-userId mismatch.
- **Verified:** Confirmed from actual code. server/websocket/connections.ts holds the real logic: registry keyed by userId (addConnection L12-16), sendToConnection guards ws.readyState===1 (L34), broadcastToAdmins filters role==='admin' (L43), broadcastToProvider does connections.get(providerId) (L54). broadcast.ts is a thin re-export barrel; every test (checkr-integration, onboarding, admin-pipeline, training-mo…

### Unescaped LIKE/ILIKE wildcards in multiple search endpoints (beyond invoice #132)
- **Location:** `server/api/routes/admin.ts:163`  
- **Dimension:** Input validation & injection · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** Several search handlers interpolate the raw `search` query param directly into ILIKE patterns without `escapeLike()`. admin.ts bookings search: `ilike(bookings.contactName, `%${search}%`)`, `ilike(bookings.contactPhone, `%${search}%`)`, `ilike(bookings.contactEmail, `%${search}%`)` (admin.ts:163-165). Same unescaped pattern in trust-tier.ts:34-35 (`ilike(users.name, `%${search}%`)`), b2b-accounts.ts:50 (`ilike(b2bAccounts.companyName, `%${search}%`)`), invoices.ts:188-190, and provider-invoices.ts:52-53. The codebase already has the fix util — sql-escape.ts:9 `escapeLike()` and its own doc warns `%foo%` from raw input "can force expensive sequential scans" — and it IS applied correctly at admin.ts:999 (`const safe = escapeLike(search)`) and user-search.ts:34. These endpoints simply forgot it.
- **Fix:** Wrap the search term with `escapeLike(search)` before building the `%...%` pattern in each of these handlers, mirroring admin.ts:999 / user-search.ts:34. Not SQL injection (drizzle parameterizes the value), but `_`/`%` broaden matches incorrectly and `%a%b%c%` inputs force expensive sequential scans (search-driven DoS).
- **Verified:** Verified in the actual code. admin.ts:163-165 interpolates raw `search` into `ilike(...,`%${search}%`)` for contactName/contactPhone/contactEmail with no escaping, while admin.ts:999 in the same file correctly does `const safe = escapeLike(search)`. The util at server/api/lib/sql-escape.ts:9 exists and its own doc warns that raw `%foo%` "can force expensive sequential scans." The same unescaped pa…

### Pagination limit/page params not clamped or NaN-guarded (resource exhaustion)
- **Location:** `server/api/routes/admin.ts:150`  
- **Dimension:** Input validation & injection · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** `const limit = parseInt(c.req.query("limit") || "20");` (admin.ts:150) — the `|| "20"` only supplies a default for a *missing* param, not for a non-numeric one, so `?limit=abc` yields `NaN` and `?limit=99999999` is accepted unbounded, with `offset = (page-1)*limit` going negative for `?page=0` or `?page=-1`. Same unbounded/NaN-prone pattern in invoices.ts:170-171, provider-invoices.ts:34-35, customer-invoices.ts:21-22, admin-invoices.ts:32-33, customer.ts:21-22, referrals.ts:65-66/367-368, and reviews.ts:38-39. The correct clamped idiom already exists in the same codebase: b2b-accounts.ts:43 / trust-tier.ts:26 use `Math.min(Math.max(parseInt(...) || 20, 1), 100)`.
- **Fix:** Apply the existing clamp idiom (`Math.min(Math.max(parseInt(...) || 20, 1), 100)` for limit and `Math.max(parseInt(...) || 1, 1)` for page) to all listed list endpoints, or centralize it in a shared pagination helper.
- **Verified:** Verified against actual code. /root/road-side-atl/server/api/routes/admin.ts:149-151 reads `page = parseInt(c.req.query("page") || "1")`, `limit = parseInt(c.req.query("limit") || "20")`, `offset = (page - 1) * limit`. The `|| "20"`/`|| "1"` defaults only fire for a missing param, not a non-numeric one, so `?limit=abc` produces NaN and `?limit=99999999` is accepted unbounded; `?page=0`/`?page=-1` …


## ⚪ LOW

### validatePaymentMethod reads c.get("user") with no null guard
- **Location:** `server/api/middleware/trust-tier.ts:14`  
- **Dimension:** AuthN/Z & middleware · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** `const user = c.get("user");` then immediately `user.id` (trust-tier.ts:14,18,30) with no null check. It is only safe today because payments.ts:26-27 mounts `requireAuth` before `validatePaymentMethod`. If this middleware is ever mounted without a preceding auth middleware, `user` is undefined and the handler throws a 500 (fails closed, but is fragile and asymmetric with the other middleware that all re-resolve/guard).
- **Fix:** Resolve via `resolveUser(c)` or add `if (!user) return c.json({ error: "Unauthorized" }, 401);` at the top, matching requireIcAgreementAccepted (onboarding.ts:62) which does guard the cast.
- **Verified:** Confirmed from /root/road-side-atl/server/api/middleware/trust-tier.ts: line 14 reads `const user = c.get("user")` with no null guard, then dereferences `user.id` at line 17 (`eq(users.id, user.id)`) and line 30 (`userId: user.id`). It is only safe because payments.ts:26-27 mounts requireAuth before validatePaymentMethod. The asymmetry is real: requireAuth/requireAdmin/requireProvider in auth.ts a…

### requireB2bMember pins an arbitrary account for multi-account users
- **Location:** `server/api/middleware/b2b-member.ts:31`  
- **Dimension:** AuthN/Z & middleware · **Category:** correctness · **Reviewer confidence:** medium
- **Evidence:** `const membership = await db.query.b2bAccountMembers.findFirst({ where: eq(b2bAccountMembers.userId, user.id) });` then sets `b2bAccountId`/`b2bRole` from it. `findFirst` has no ordering, so a user who is a member of multiple B2B accounts is silently bound to a non-deterministic account for the whole portal session, with no way to select the intended tenant (all b2b-portal queries scope to this value). Not cross-tenant escalation, but can cause actions/credit spend against the wrong account.
- **Fix:** Disambiguate the account explicitly — e.g. accept a client-provided accountId and verify membership for it, or order deterministically and expose an account switcher; otherwise the portal silently operates on whichever membership row is returned first.
- **Verified:** Confirmed from code. server/api/middleware/b2b-member.ts:28-30 calls findFirst with no orderBy and pins b2bAccountId/b2bRole from the first row. db/schema/b2b-members.ts:28 has a uniqueIndex only on (accountId, userId), so a user can be a member of multiple B2B accounts, and b2b-portal.ts:290-305 (POST /members) lets owners attach an existing user to their account, making the multi-account state r…

### Repeated 'find provider by userId or 404' boilerplate across provider/onboarding routes
- **Location:** `server/api/routes/provider.ts:43`  
- **Dimension:** Large-file maintainability · **Category:** maintainability · **Reviewer confidence:** high
- **Evidence:** The exact block `const provider = await db.query.providers.findFirst({ where: eq(providers.userId, user.id) }); if (!provider) return c.json({ error: "Provider ... not found" }, 404);` is repeated in nearly every handler — provider.ts lines 43, 88, 121, 162, 230, 328, 398, 454, 484, 537, 558, 665, 741, 782, 831, 942 (and again with `columns:{id:true}` variants at 987, 1021, 1076, 1103, 1157, 1194, 1233, 1281), plus the same pattern throughout onboarding.ts. The error string even varies ('Provider profile not found' vs 'Provider not found') for the same condition.
- **Fix:** Add a middleware layered after `requireProvider` that resolves the provider once and sets `c.set("provider", provider)` (404 if missing), so handlers read `c.get("provider")`. This removes ~25 duplicated lookups and unifies the error message.
- **Verified:** Confirmed from actual code. The block `const provider = await db.query.providers.findFirst({ where: eq(providers.userId, user.id) }); if (!provider) return c.json({ error: ... }, 404)` appears verbatim at server/api/routes/provider.ts:43-49 and is duplicated across ~25+ handlers (grep shows providers.findFirst at lines 43, 88, 121, 162, 230, 328, 398, 454, 484, 537, 558, 665, 741, 782, 831, 942, 9…

### B2bAccountsTable (1070 lines) bundles 5 dialogs and 8 repetitive fetch handlers in one component
- **Location:** `components/admin/b2b-accounts-table.tsx:98`  
- **Dimension:** Large-file maintainability · **Category:** maintainability · **Reviewer confidence:** high
- **Evidence:** A single client component holds 5 distinct dialog UIs (create/edit, detail, contract, invoices, booking) plus 8 mutation handlers (`handleCreate`, `handleUpdate`, `handleContractUpdate`, `handleStatusChange`, `handleCreateBooking`, `handleGenerateInvoice`, `handleSendInvoice`, `handleInvoiceStatusChange`) that all repeat the identical shape: `setSaving(true)` / `fetch(...)` / `if(!res.ok){throw new Error(err.error||...)}` / `toast.success` / `fetchAccounts()` / `catch{toast.error(...)}` / `finally{setSaving(false)}` (lines 271-524). The component also juggles four separate form-state objects.
- **Fix:** Extract a `mutate(url, {method, body, success})` helper that encapsulates the fetch/toast/error envelope (collapses ~8 handlers to one-liners), and split each dialog into its own component (`B2bAccountDialog`, `B2bContractDialog`, `B2bInvoicesDialog`, `B2bBookingDialog`) receiving the selected account + an onSaved callback.
- **Verified:** Confirmed from the actual code at /root/road-side-atl/components/admin/b2b-accounts-table.tsx. The file is exactly 1070 lines. Lines 271-524 contain all 8 cited mutation handlers (handleCreate, handleUpdate, handleContractUpdate, handleStatusChange, handleCreateBooking, handleGenerateInvoice, handleSendInvoice, handleInvoiceStatusChange), each duplicating the identical fetch/toast/error envelope: …

### Outbound webhook HMAC has no timestamp — payloads are replayable
- **Location:** `server/api/lib/outbound-webhooks.ts:69`  
- **Dimension:** Notifications & outbound webhooks · **Category:** security · **Reviewer confidence:** high
- **Evidence:** The signature covers only the JSON body: `signPayload(sub.secret, body)` where `body = JSON.stringify({ event, data, deliveryId })` (outbound-webhooks.ts:69, 76) and `signPayload` is `crypto.createHmac('sha256', secret).update(body).digest('hex')` (lines 18-20). No `x-roadsidega-timestamp` header is sent and no timestamp is included in the signed material, so a captured valid request can be replayed indefinitely against the partner endpoint — the receiver has no way to bound request freshness. Standard webhook signing (Stripe/Svix style) signs `timestamp.body` and sends the timestamp header.
- **Fix:** Add an `x-roadsidega-timestamp` header, sign `${timestamp}.${body}`, and document a tolerance window so receivers can reject stale/replayed deliveries.
- **Verified:** Confirmed in code. server/api/lib/outbound-webhooks.ts:18-20 signs only the body (`createHmac('sha256', secret).update(body)`); line 69 builds `body = JSON.stringify({ event, data, deliveryId })` with no timestamp; lines 73-77 send `x-roadsidega-signature` but no timestamp header. So there is no signed timestamp and no freshness bound — the core claim is accurate and the Stripe/Svix-style fix is a…

### Webhook dead-lettering and inactive-subscription drops produce no alert
- **Location:** `server/api/lib/outbound-webhooks.ts:90`  
- **Dimension:** Notifications & outbound webhooks · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** When a delivery exhausts retries it is silently marked failed: `if (attempts >= MAX_ATTEMPTS) { await db.update(...).set({ status: 'failed', lastError: msg }) ... }` (outbound-webhooks.ts:90-91), and a delivery whose subscription went inactive is dropped with `status: 'failed', lastError: 'subscription inactive'` (line 65). Neither path emits a log line or ops alert — permanent loss of a partner event (e.g. booking.created) is invisible unless someone queries the table. The cron only logs aggregate `{ attempted, delivered }` (cron.ts:151-152), which hides dead-letters.
- **Fix:** Emit `logger.error`/ops alert on each dead-letter and surface a dead-letter count in the cron result so permanent delivery failures are observable.
- **Verified:** Confirmed from /root/road-side-atl/server/api/lib/outbound-webhooks.ts. The dead-letter path (lines 90-91) sets status:'failed'/lastError:msg with no logger or ops-alert call, and the inactive-subscription drop (lines 64-65) sets status:'failed'/lastError:'subscription inactive' also silently. deliverPendingWebhooks returns only {attempted, delivered} (line 98) with no failed/dead-letter count, an…

### processPendingPayouts 'skips held payouts' is dead code; skippedHeld never incremented
- **Location:** `server/api/lib/reconciliation.ts:669`  
- **Dimension:** Reconciliation scheduled jobs · **Category:** maintainability · **Reviewer confidence:** high
- **Evidence:** The docstring (line 636) states it 'Skips payouts that are "held" (dispute freeze)', and the function declares `const skippedHeld = 0;` which is returned unchanged. The query only filters `eq(providerPayouts.status, 'pending')` and `payoutMethod = 'stripe_connect'`; held payouts are excluded only incidentally because `held` is a separate enum status (provider-payouts.ts:7). The schema also carries `holdReason`/`heldAt` (lines 30-31) independent of status — if a hold is ever expressed via those fields while status remains `pending`, this job would transfer the frozen funds, and the reported `skippedHeld` count would still be 0, masking it.
- **Fix:** Either remove the misleading docstring/`skippedHeld` field, or make the hold check explicit: filter out rows with `holdReason IS NOT NULL`/`heldAt IS NOT NULL` and increment `skippedHeld` so frozen payouts are provably excluded and observable.
- **Verified:** Confirmed in code. reconciliation.ts:669 declares `const skippedHeld = 0;` which is never reassigned (loop at 671-714 only touches processed/failed) and returned unchanged at 664/717 — it is dead/vestigial and always reports 0, providing zero observability. The docstring at line 636 ("Skips payouts that are 'held'") implies an explicit hold check that does not exist; the query (655-659) filters on…

### DB-backed rate limiting and login lockout fail open on any DB error
- **Location:** `server/api/middleware/rate-limit.ts:122`  
- **Dimension:** Secrets, rate-limiting, PII · **Category:** security · **Reviewer confidence:** medium
- **Evidence:** In rateLimitDb: `catch (err) { console.error("[RateLimit] DB store error — failing open:", err); return next(); }` (server/api/middleware/rate-limit.ts:122-127). The login lockout gate also fails open: `peekRateLimitDb` returns true on error (`catch (err) { console.error("[RateLimit] peek failed — allowing:", err); return true; }`, server/api/lib/rate-limiter-db.ts:64-67), and assertLoginAllowed relies on it (lib/auth/login-throttle.ts:41-47). These cover the most sensitive endpoints (auth /register, /forgot-password, /reset-password, /mobile-login and the NextAuth credentials login). Any DB hiccup or unmigrated rateLimits table silently removes all brute-force/credential-stuffing protection.
- **Fix:** Acceptable as a deliberate availability tradeoff, but bound it: emit an alert on the fail-open path and consider a short in-memory fallback cap (e.g. via the existing in-memory limiter) so auth endpoints are never completely unthrottled during a DB outage.
- **Verified:** Code confirmed at all cited locations. rateLimitDb (rate-limit.ts:122-127) fails open (console.error + next()) on any DB error; peekRateLimitDb (rate-limiter-db.ts:64-67) returns true on error; assertLoginAllowed (login-throttle.ts:41-47) gates on that peek, so login throttling is bypassed on error. The durable variants rateLimitAuthDb/rateLimitStrictDb are the only protection on those auth endpoi…

### Dual rate-limit backends diverge: provider registration uses the non-durable in-memory limiter
- **Location:** `server/api/routes/provider-registration.ts:23`  
- **Dimension:** Secrets, rate-limiting, PII · **Category:** reliability · **Reviewer confidence:** high
- **Evidence:** `app.use("/*", rateLimitAuth)` applies the in-memory limiter (server/api/middleware/rate-limit.ts:157) to the provider self-registration surface, while the equivalent account-auth endpoints use the durable Postgres-backed variant: auth.ts:26-30 wire `rateLimitAuthDb`/`rateLimitStrictDb`. So registration abuse counters reset on every deploy and aren't shared across instances, unlike /register — an inconsistent, weaker guard for an endpoint of comparable sensitivity. The two backends also live in separate stores with no shared key namespace.
- **Fix:** Switch provider-registration to the durable rateLimitDb-based middleware (and ideally an email/invite-token key) so both registration paths share the same durable enforcement.
- **Verified:** Confirmed from code. provider-registration.ts:23 uses `app.use("/*", rateLimitAuth)`, which is the in-memory limiter (rate-limit.ts:157 -> rate-limit() using checkRateLimit from the in-memory rate-limiter). auth.ts:26-30 wire rateLimitAuthDb/rateLimitStrictDb (rate-limit.ts:162-163), the durable Postgres-backed variant (checkRateLimitDb) that also keys on email. The two backends are indeed separat…

### AUTH_SECRET allows a 16-char secret and is excluded from the production placeholder/strength guard
- **Location:** `lib/env.ts:13`  
- **Dimension:** Secrets, rate-limiting, PII · **Category:** security · **Reviewer confidence:** medium
- **Evidence:** `AUTH_SECRET: z.string().min(16, ...)` permits a low-entropy 16-character session/JWT signing secret (NextAuth signs sessions with this; recommended >=32 random bytes). The production fail-closed guard (lib/env.ts:78-96) validates only STRIPE/RESEND/GOOGLE placeholders and never checks AUTH_SECRET, ENCRYPTION_KEY, or TWILIO_AUTH_TOKEN for placeholder/weak values, so a deploy with a default-y AUTH_SECRET passes validation.
- **Fix:** Raise the AUTH_SECRET minimum (e.g. .min(32)) and add it (plus ENCRYPTION_KEY and TWILIO_AUTH_TOKEN) to the production placeholder/strength checks.
- **Verified:** Confirmed from /root/road-side-atl/lib/env.ts. Line 13 defines `AUTH_SECRET: z.string().min(16, ...)`, which permits a 16-char (low-entropy) session/JWT signing secret versus the >=32-byte best practice. The production fail-closed guard at lines 78-96 only checks STRIPE_SECRET_KEY, STRIPE_WEBHOOK_SECRET, RESEND_API_KEY, AUTH_GOOGLE_ID, and AUTH_GOOGLE_SECRET for placeholder values; it never valida…

### Non-atomic event dedup (check-at-start, mark-at-end, both fail-open) allows concurrent redeliveries to double-execute non-constraint-guarded side effects
- **Location:** `server/api/routes/webhooks.ts:134`  
- **Dimension:** Stripe webhooks & idempotency · **Category:** correctness · **Reviewer confidence:** high
- **Evidence:** Dedup is a read-then-act-then-mark sequence: `if (await isEventProcessed(event.id, "stripe")) {...}` at line 134, full side-effect switch, then `await markEventProcessed(...)` at line 860. There is no atomic claim (e.g. insert-first onConflictDoNothing gating execution). Both helpers also fail open (markEventProcessed swallows its insert error at lines 55-57; isEventProcessed returns false on error at line 67). So two concurrent deliveries of the same event id (Stripe retries while the first is still in-flight) both pass the guard and run the whole body. The payout/clawback writes are backstopped by unique indexes, but unguarded side effects double-fire: charge.dispute.created calls `demoteTrustTier(...)` (line 369) twice (double trust-tier demotion), payout/dispute notifications fire twice, and booking notes/finalPrice updates re-run.
- **Fix:** Make the claim atomic and gating: insert into webhook_events with onConflictDoNothing as the FIRST step and only proceed if the insert created the row; on unexpected processing failure, delete the claim (or use status=failed) so Stripe can retry. Make idempotent non-DB side effects (trust-tier demotion) guarded by a per-event/per-dispute marker.
- **Verified:** Confirmed against the code: dedup is non-atomic (isEventProcessed at webhooks.ts:134, markEventProcessed at :860 — no insert-first gating claim) and both helpers fail open (markEventProcessed swallows errors at :54-56, isEventProcessed returns false on error at :65-67), so two concurrent deliveries of the same event id can both pass the guard and run the full body. demoteTrustTier IS invoked fire-…
